target/

# Node / pnpm workspace (hoisted to the repo root by pnpm-workspace.yaml).
node_modules/

# wasm-pack output for the price-core WASM package (built via `make wasm`).
sdk/price-core/pkg/

# Track the vanity program keypair.
!/target/
/target/*
!/target/deploy/
/target/deploy/*
!/target/deploy/dropset_gamma-keypair.json

# Local audit findings (see .claude/skills/audit-codebase).
.audits/

# Background audit-loop state (see .claude/skills/platform-audit-loop).
.audit-loop/

# macOS metadata.
.DS_Store
