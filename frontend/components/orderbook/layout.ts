// Row metrics shared by the depth ladder and the trades tape. The two panes
// stack directly on top of each other, so a divergence here reads as a
// misalignment between them rather than as two independent choices.

// Height of one data row in either pane.
export const ROW_H = "h-[22px]";
