/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getAddressDecoder, getAddressEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder } from '@solana/kit';

/**
 * A flat fee charged in `mint`, paid to the registry fee ATA. Mirrors
 * the `FeeConfig` in the architecture spec; reused per-market by the
 * `MarketHeader` once markets exist. Carrying `token_program`
 * alongside `mint` lets downstream fee-collection paths derive the
 * canonical ATA — `(authority, token_program, mint)` — without
 * guessing which token program owns the mint.
 */
export type FeeConfig = { 
/** Mint accepted for this fee. */
mint: Address; 
/** Token program owning `mint` — SPL Token or Token-2022. */
tokenProgram: Address; 
/** Amount in atoms of `mint`. */
atoms: bigint;  };

export type FeeConfigArgs = { 
/** Mint accepted for this fee. */
mint: Address; 
/** Token program owning `mint` — SPL Token or Token-2022. */
tokenProgram: Address; 
/** Amount in atoms of `mint`. */
atoms: number | bigint;  };

export function getFeeConfigEncoder(): FixedSizeEncoder<FeeConfigArgs> {
    return getStructEncoder([['mint', getAddressEncoder()], ['tokenProgram', getAddressEncoder()], ['atoms', getU64Encoder()]]);
}

export function getFeeConfigDecoder(): FixedSizeDecoder<FeeConfig> {
    return getStructDecoder([['mint', getAddressDecoder()], ['tokenProgram', getAddressDecoder()], ['atoms', getU64Decoder()]]);
}

export function getFeeConfigCodec(): FixedSizeCodec<FeeConfigArgs, FeeConfig> {
    return combineCodec(getFeeConfigEncoder(), getFeeConfigDecoder());
}