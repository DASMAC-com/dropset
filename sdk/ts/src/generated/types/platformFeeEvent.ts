/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getAddressDecoder, getAddressEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU64Decoder, getU64Encoder, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder } from '@solana/kit';

export type PlatformFeeEvent = { market: Address; taker: Address; 
/** Owner of the destination token account — the integrator being paid. */
feeAuthority: Address; 
/**
 * Mint the fee was paid in: the swap's **output** leg (base on a Buy,
 * quote on a Sell), so a consumer needn't re-derive it from `side`.
 */
mint: Address; 
/** Atoms transferred, after the taker fee and rounded down. */
atoms: bigint; 
/**
 * The rate the caller declared, in bps — recorded alongside `atoms`
 * so the rounding is auditable from the event alone.
 */
platformFeeBps: number;  };

export type PlatformFeeEventArgs = { market: Address; taker: Address; 
/** Owner of the destination token account — the integrator being paid. */
feeAuthority: Address; 
/**
 * Mint the fee was paid in: the swap's **output** leg (base on a Buy,
 * quote on a Sell), so a consumer needn't re-derive it from `side`.
 */
mint: Address; 
/** Atoms transferred, after the taker fee and rounded down. */
atoms: number | bigint; 
/**
 * The rate the caller declared, in bps — recorded alongside `atoms`
 * so the rounding is auditable from the event alone.
 */
platformFeeBps: number;  };

export function getPlatformFeeEventEncoder(): FixedSizeEncoder<PlatformFeeEventArgs> {
    return getStructEncoder([['market', getAddressEncoder()], ['taker', getAddressEncoder()], ['feeAuthority', getAddressEncoder()], ['mint', getAddressEncoder()], ['atoms', getU64Encoder()], ['platformFeeBps', getU16Encoder()]]);
}

export function getPlatformFeeEventDecoder(): FixedSizeDecoder<PlatformFeeEvent> {
    return getStructDecoder([['market', getAddressDecoder()], ['taker', getAddressDecoder()], ['feeAuthority', getAddressDecoder()], ['mint', getAddressDecoder()], ['atoms', getU64Decoder()], ['platformFeeBps', getU16Decoder()]]);
}

export function getPlatformFeeEventCodec(): FixedSizeCodec<PlatformFeeEventArgs, PlatformFeeEvent> {
    return combineCodec(getPlatformFeeEventEncoder(), getPlatformFeeEventDecoder());
}