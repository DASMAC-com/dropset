/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getAddressDecoder, getAddressEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder } from '@solana/kit';

export type CloseRegistryFeeVaultEvent = { 
/**
 * Mint of the fee vault closed — the discriminator across a
 * registry's several historical fee ATAs.
 */
feeMint: Address; 
/** Token account the collected fees were paid to. */
tokenRecipient: Address; 
/** Address the fee vault's rent lamports were paid to. */
rentRecipient: Address; 
/**
 * `fee_vault.amount` read immediately before the drain: the fees this
 * vault accumulated over its life, plus anything sent to it
 * unsolicited. The registry tracks no per-mint counter to split those
 * two apart, so unlike the market-treasury event there is no accrued
 * term to carry.
 */
collected: bigint;  };

export type CloseRegistryFeeVaultEventArgs = { 
/**
 * Mint of the fee vault closed — the discriminator across a
 * registry's several historical fee ATAs.
 */
feeMint: Address; 
/** Token account the collected fees were paid to. */
tokenRecipient: Address; 
/** Address the fee vault's rent lamports were paid to. */
rentRecipient: Address; 
/**
 * `fee_vault.amount` read immediately before the drain: the fees this
 * vault accumulated over its life, plus anything sent to it
 * unsolicited. The registry tracks no per-mint counter to split those
 * two apart, so unlike the market-treasury event there is no accrued
 * term to carry.
 */
collected: number | bigint;  };

export function getCloseRegistryFeeVaultEventEncoder(): FixedSizeEncoder<CloseRegistryFeeVaultEventArgs> {
    return getStructEncoder([['feeMint', getAddressEncoder()], ['tokenRecipient', getAddressEncoder()], ['rentRecipient', getAddressEncoder()], ['collected', getU64Encoder()]]);
}

export function getCloseRegistryFeeVaultEventDecoder(): FixedSizeDecoder<CloseRegistryFeeVaultEvent> {
    return getStructDecoder([['feeMint', getAddressDecoder()], ['tokenRecipient', getAddressDecoder()], ['rentRecipient', getAddressDecoder()], ['collected', getU64Decoder()]]);
}

export function getCloseRegistryFeeVaultEventCodec(): FixedSizeCodec<CloseRegistryFeeVaultEventArgs, CloseRegistryFeeVaultEvent> {
    return combineCodec(getCloseRegistryFeeVaultEventEncoder(), getCloseRegistryFeeVaultEventDecoder());
}