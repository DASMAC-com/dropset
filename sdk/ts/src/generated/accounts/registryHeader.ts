/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU32Decoder, getU32Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getFeeConfigDecoder, getFeeConfigEncoder, type FeeConfig, type FeeConfigArgs } from '../types';

export const REGISTRY_HEADER_DISCRIMINATOR = new Uint8Array([74, 214, 198, 41, 138, 211, 36, 245]);

export function getRegistryHeaderDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(REGISTRY_HEADER_DISCRIMINATOR); }

export type RegistryHeader = { discriminator: ReadonlyUint8Array; 
/**
 * Default fee for the per-`CreateVault` charge, stamped into
 * `MarketHeader.fee_config` at market creation.
 */
defaultFeeConfig: FeeConfig; 
/**
 * Number of live markets created against this registry. Incremented
 * by `create_market`, decremented by `close_market`. `close_registry`
 * requires this to be zero — the only on-chain witness that no
 * orphan markets remain, since the program cannot iterate all PDAs
 * to verify by enumeration. See the architecture spec, **Account
 * lifecycle and rent reclamation**.
 */
marketCount: number; 
/** Default skin-in-the-game floor (ppm, [`Ppm32`]) stamped into markets. */
defaultMinLeaderShare: number; 
/** Default taker fee (ppm, [`Ppm16`]) stamped into markets. */
defaultTakerFee: number; 
/** Default cap on vaults per market. */
maxVaultsPerMarket: number; 
/** Registry PDA bump. */
bump: number;  };

export type RegistryHeaderArgs = { 
/**
 * Default fee for the per-`CreateVault` charge, stamped into
 * `MarketHeader.fee_config` at market creation.
 */
defaultFeeConfig: FeeConfigArgs; 
/**
 * Number of live markets created against this registry. Incremented
 * by `create_market`, decremented by `close_market`. `close_registry`
 * requires this to be zero — the only on-chain witness that no
 * orphan markets remain, since the program cannot iterate all PDAs
 * to verify by enumeration. See the architecture spec, **Account
 * lifecycle and rent reclamation**.
 */
marketCount: number; 
/** Default skin-in-the-game floor (ppm, [`Ppm32`]) stamped into markets. */
defaultMinLeaderShare: number; 
/** Default taker fee (ppm, [`Ppm16`]) stamped into markets. */
defaultTakerFee: number; 
/** Default cap on vaults per market. */
maxVaultsPerMarket: number; 
/** Registry PDA bump. */
bump: number;  };

/** Gets the encoder for {@link RegistryHeaderArgs} account data. */
export function getRegistryHeaderEncoder(): FixedSizeEncoder<RegistryHeaderArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['defaultFeeConfig', getFeeConfigEncoder()], ['marketCount', getU32Encoder()], ['defaultMinLeaderShare', getU32Encoder()], ['defaultTakerFee', getU16Encoder()], ['maxVaultsPerMarket', getU8Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: REGISTRY_HEADER_DISCRIMINATOR }));
}

/** Gets the decoder for {@link RegistryHeader} account data. */
export function getRegistryHeaderDecoder(): FixedSizeDecoder<RegistryHeader> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['defaultFeeConfig', getFeeConfigDecoder()], ['marketCount', getU32Decoder()], ['defaultMinLeaderShare', getU32Decoder()], ['defaultTakerFee', getU16Decoder()], ['maxVaultsPerMarket', getU8Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link RegistryHeader} account data. */
export function getRegistryHeaderCodec(): FixedSizeCodec<RegistryHeaderArgs, RegistryHeader> {
    return combineCodec(getRegistryHeaderEncoder(), getRegistryHeaderDecoder());
}

export function decodeRegistryHeader<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<RegistryHeader, TAddress>;
export function decodeRegistryHeader<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<RegistryHeader, TAddress>;
export function decodeRegistryHeader<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<RegistryHeader, TAddress> | MaybeAccount<RegistryHeader, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getRegistryHeaderDecoder());
}

export async function fetchRegistryHeader<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RegistryHeader, TAddress>> {
  const maybeAccount = await fetchMaybeRegistryHeader(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRegistryHeader<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RegistryHeader, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRegistryHeader(maybeAccount);
}

export async function fetchAllRegistryHeader(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RegistryHeader>[]> {
  const maybeAccounts = await fetchAllMaybeRegistryHeader(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRegistryHeader(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RegistryHeader>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRegistryHeader(maybeAccount));
}

export function getRegistryHeaderSize(): number {
  return 92;
}