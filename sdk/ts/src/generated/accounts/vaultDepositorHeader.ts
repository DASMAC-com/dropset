/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getPriceDecoder, getPriceEncoder, type Price, type PriceArgs } from '../types';

export const VAULT_DEPOSITOR_HEADER_DISCRIMINATOR = new Uint8Array([249, 190, 164, 120, 230, 61, 124, 175]);

export function getVaultDepositorHeaderDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DEPOSITOR_HEADER_DISCRIMINATOR); }

export type VaultDepositorHeader = { discriminator: ReadonlyUint8Array; 
/**
 * Market PDA this position is on. The vault is identified by
 * `(market, sector_idx)`; the PDA seeds include both so a sector
 * reuse across vault lifetimes derives a fresh address.
 */
market: Address; 
/** Sector index within the market's slab. */
sectorIdx: number; 
/**
 * Depositor wallet — bound by the PDA seeds, so the account is
 * non-transferable (there is no separate authority field to
 * reassign). See the spec's **Shares**.
 */
owner: Address; 
/**
 * Pro-rata claim on the vault — the per-depositor term of
 * invariant I6 (`leader_shares + Σ VaultDepositor.shares ==
 * total_shares`).
 */
shares: bigint; 
/**
 * Quote-denominated principal of the **remaining** position. Spec
 * formula (`Deposit` top-off): `Σ (quote_in + base_in × ref_now)`.
 * Reduced on `Withdraw` by the floored share-pro-rata slice.
 */
netDeposits: bigint; 
/**
 * Lifetime contributions, monotonic — **never reduced on
 * withdraw**. Stable denominator for all-time return %.
 */
grossDeposited: bigint; 
/** Shares-weighted average reference price across deposits. */
entryRefPrice: Price; 
/**
 * Shares-weighted average VPS (`L / total_shares`) across deposits,
 * Q32.32. Same encoding and practical bound as [`crate::Vault::hwm`].
 */
entryVps: bigint; 
/** Slot of the first deposit. Captured once at PDA init. */
openedAt: bigint; 
/**
 * Signed quote-denominated PnL crystallized by past withdrawals.
 * Discarded when the account is closed at zero shares.
 */
realizedPnl: bigint; 
/** Signed yield (ex-FX) component of [`Self::realized_pnl`]. */
realizedYield: bigint; 
/**
 * Signed FX component. Invariant:
 * `realized_yield + realized_fx == realized_pnl`.
 */
realizedFx: bigint; 
/** PDA bump. */
bump: number; 
/**
 * Padding so the on-chain size is a multiple of 8 and the
 * `#[account]` derive's discriminator + body length matches the
 * const-asserted total below.
 */
reserved: ReadonlyUint8Array;  };

export type VaultDepositorHeaderArgs = { 
/**
 * Market PDA this position is on. The vault is identified by
 * `(market, sector_idx)`; the PDA seeds include both so a sector
 * reuse across vault lifetimes derives a fresh address.
 */
market: Address; 
/** Sector index within the market's slab. */
sectorIdx: number; 
/**
 * Depositor wallet — bound by the PDA seeds, so the account is
 * non-transferable (there is no separate authority field to
 * reassign). See the spec's **Shares**.
 */
owner: Address; 
/**
 * Pro-rata claim on the vault — the per-depositor term of
 * invariant I6 (`leader_shares + Σ VaultDepositor.shares ==
 * total_shares`).
 */
shares: number | bigint; 
/**
 * Quote-denominated principal of the **remaining** position. Spec
 * formula (`Deposit` top-off): `Σ (quote_in + base_in × ref_now)`.
 * Reduced on `Withdraw` by the floored share-pro-rata slice.
 */
netDeposits: number | bigint; 
/**
 * Lifetime contributions, monotonic — **never reduced on
 * withdraw**. Stable denominator for all-time return %.
 */
grossDeposited: number | bigint; 
/** Shares-weighted average reference price across deposits. */
entryRefPrice: PriceArgs; 
/**
 * Shares-weighted average VPS (`L / total_shares`) across deposits,
 * Q32.32. Same encoding and practical bound as [`crate::Vault::hwm`].
 */
entryVps: number | bigint; 
/** Slot of the first deposit. Captured once at PDA init. */
openedAt: number | bigint; 
/**
 * Signed quote-denominated PnL crystallized by past withdrawals.
 * Discarded when the account is closed at zero shares.
 */
realizedPnl: number | bigint; 
/** Signed yield (ex-FX) component of [`Self::realized_pnl`]. */
realizedYield: number | bigint; 
/**
 * Signed FX component. Invariant:
 * `realized_yield + realized_fx == realized_pnl`.
 */
realizedFx: number | bigint; 
/** PDA bump. */
bump: number; 
/**
 * Padding so the on-chain size is a multiple of 8 and the
 * `#[account]` derive's discriminator + body length matches the
 * const-asserted total below.
 */
reserved: ReadonlyUint8Array;  };

/** Gets the encoder for {@link VaultDepositorHeaderArgs} account data. */
export function getVaultDepositorHeaderEncoder(): FixedSizeEncoder<VaultDepositorHeaderArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['market', getAddressEncoder()], ['sectorIdx', getU32Encoder()], ['owner', getAddressEncoder()], ['shares', getU64Encoder()], ['netDeposits', getU64Encoder()], ['grossDeposited', getU64Encoder()], ['entryRefPrice', getPriceEncoder()], ['entryVps', getU64Encoder()], ['openedAt', getU64Encoder()], ['realizedPnl', getI64Encoder()], ['realizedYield', getI64Encoder()], ['realizedFx', getI64Encoder()], ['bump', getU8Encoder()], ['reserved', fixEncoderSize(getBytesEncoder(), 7)]]), (value) => ({ ...value, discriminator: VAULT_DEPOSITOR_HEADER_DISCRIMINATOR }));
}

/** Gets the decoder for {@link VaultDepositorHeader} account data. */
export function getVaultDepositorHeaderDecoder(): FixedSizeDecoder<VaultDepositorHeader> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['market', getAddressDecoder()], ['sectorIdx', getU32Decoder()], ['owner', getAddressDecoder()], ['shares', getU64Decoder()], ['netDeposits', getU64Decoder()], ['grossDeposited', getU64Decoder()], ['entryRefPrice', getPriceDecoder()], ['entryVps', getU64Decoder()], ['openedAt', getU64Decoder()], ['realizedPnl', getI64Decoder()], ['realizedYield', getI64Decoder()], ['realizedFx', getI64Decoder()], ['bump', getU8Decoder()], ['reserved', fixDecoderSize(getBytesDecoder(), 7)]]);
}

/** Gets the codec for {@link VaultDepositorHeader} account data. */
export function getVaultDepositorHeaderCodec(): FixedSizeCodec<VaultDepositorHeaderArgs, VaultDepositorHeader> {
    return combineCodec(getVaultDepositorHeaderEncoder(), getVaultDepositorHeaderDecoder());
}

export function decodeVaultDepositorHeader<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<VaultDepositorHeader, TAddress>;
export function decodeVaultDepositorHeader<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<VaultDepositorHeader, TAddress>;
export function decodeVaultDepositorHeader<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<VaultDepositorHeader, TAddress> | MaybeAccount<VaultDepositorHeader, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getVaultDepositorHeaderDecoder());
}

export async function fetchVaultDepositorHeader<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VaultDepositorHeader, TAddress>> {
  const maybeAccount = await fetchMaybeVaultDepositorHeader(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVaultDepositorHeader<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VaultDepositorHeader, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVaultDepositorHeader(maybeAccount);
}

export async function fetchAllVaultDepositorHeader(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VaultDepositorHeader>[]> {
  const maybeAccounts = await fetchAllMaybeVaultDepositorHeader(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVaultDepositorHeader(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VaultDepositorHeader>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVaultDepositorHeader(maybeAccount));
}

export function getVaultDepositorHeaderSize(): number {
  return 148;
}