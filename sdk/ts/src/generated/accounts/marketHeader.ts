/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getFeeConfigDecoder, getFeeConfigEncoder, type FeeConfig, type FeeConfigArgs } from '../types';

export const MARKET_HEADER_DISCRIMINATOR = new Uint8Array([56, 105, 191, 242, 226, 243, 198, 164]);

export function getMarketHeaderDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(MARKET_HEADER_DISCRIMINATOR); }

export type MarketHeader = { discriminator: ReadonlyUint8Array; 
/** Per-fill / per-quote monotonic counter. */
nonce: bigint; 
/**
 * Head of the active DLL: sector index or [`NULL_SECTOR`]. Walked
 * by the matching engine on every taker.
 */
head: number; 
/**
 * Head of the tombstone DLL: sectors that have been `CloseVault`'d
 * but still hold outstanding shares. Not visited by matching.
 */
tombstoneHead: number; 
/**
 * Head of the free DLL: sectors available for reuse on `CreateVault`.
 * Singly linked via `next`; `prev` is ignored.
 */
freeHead: number; 
/** Active-DLL length. Bounded by `registry.max_vaults_per_market`. */
activeCount: number; 
/**
 * Number of live `VaultDepositor` PDAs across every vault on this
 * market (active and tombstoned). Incremented when an outside
 * `Deposit` opens a fresh `VaultDepositor`, decremented when
 * `Withdraw` closes one on `shares == 0` and when
 * `force_withdraw_depositor` closes one. **Not** incremented on
 * top-off (existing `VaultDepositor`). `close_market` requires
 * this to be zero — the only on-chain witness that no orphan
 * depositor PDAs remain, since the program cannot iterate all
 * PDAs to verify by enumeration. See the architecture spec,
 * **Account lifecycle and rent reclamation**.
 */
outstandingVaultDepositors: number; 
/**
 * Per-market create-vault fee: mint and amount. Seeded from
 * `Registry.default_fee_config` at market creation.
 */
feeConfig: FeeConfig; 
/** Taker fee rate, capped at ~6.55% (`Ppm16` max). */
takerFee: number; 
/**
 * Default min-leader-share for vaults opened on this market.
 * Stamped from `Registry.default_min_leader_share` at creation.
 */
defaultMinLeaderShare: number; 
/** Base leg mint. */
baseMint: Address; 
/** Quote leg mint. */
quoteMint: Address; 
/**
 * SPL / Token-2022 token account holding pooled base inventory.
 * ATA derived from `(market_pda, base_mint, base_token_program)`.
 */
baseTreasury: Address; 
/** Same as `base_treasury`, for the quote leg. */
quoteTreasury: Address; 
/** Market PDA bump. */
bump: number; 
/**
 * Bump for the base treasury ATA derivation. Stored so transfers
 * out can sign with the market PDA without re-deriving.
 */
baseTreasuryBump: number; 
/** Bump for the quote treasury ATA derivation. */
quoteTreasuryBump: number;  };

export type MarketHeaderArgs = { 
/** Per-fill / per-quote monotonic counter. */
nonce: number | bigint; 
/**
 * Head of the active DLL: sector index or [`NULL_SECTOR`]. Walked
 * by the matching engine on every taker.
 */
head: number; 
/**
 * Head of the tombstone DLL: sectors that have been `CloseVault`'d
 * but still hold outstanding shares. Not visited by matching.
 */
tombstoneHead: number; 
/**
 * Head of the free DLL: sectors available for reuse on `CreateVault`.
 * Singly linked via `next`; `prev` is ignored.
 */
freeHead: number; 
/** Active-DLL length. Bounded by `registry.max_vaults_per_market`. */
activeCount: number; 
/**
 * Number of live `VaultDepositor` PDAs across every vault on this
 * market (active and tombstoned). Incremented when an outside
 * `Deposit` opens a fresh `VaultDepositor`, decremented when
 * `Withdraw` closes one on `shares == 0` and when
 * `force_withdraw_depositor` closes one. **Not** incremented on
 * top-off (existing `VaultDepositor`). `close_market` requires
 * this to be zero — the only on-chain witness that no orphan
 * depositor PDAs remain, since the program cannot iterate all
 * PDAs to verify by enumeration. See the architecture spec,
 * **Account lifecycle and rent reclamation**.
 */
outstandingVaultDepositors: number; 
/**
 * Per-market create-vault fee: mint and amount. Seeded from
 * `Registry.default_fee_config` at market creation.
 */
feeConfig: FeeConfigArgs; 
/** Taker fee rate, capped at ~6.55% (`Ppm16` max). */
takerFee: number; 
/**
 * Default min-leader-share for vaults opened on this market.
 * Stamped from `Registry.default_min_leader_share` at creation.
 */
defaultMinLeaderShare: number; 
/** Base leg mint. */
baseMint: Address; 
/** Quote leg mint. */
quoteMint: Address; 
/**
 * SPL / Token-2022 token account holding pooled base inventory.
 * ATA derived from `(market_pda, base_mint, base_token_program)`.
 */
baseTreasury: Address; 
/** Same as `base_treasury`, for the quote leg. */
quoteTreasury: Address; 
/** Market PDA bump. */
bump: number; 
/**
 * Bump for the base treasury ATA derivation. Stored so transfers
 * out can sign with the market PDA without re-deriving.
 */
baseTreasuryBump: number; 
/** Bump for the quote treasury ATA derivation. */
quoteTreasuryBump: number;  };

/** Gets the encoder for {@link MarketHeaderArgs} account data. */
export function getMarketHeaderEncoder(): FixedSizeEncoder<MarketHeaderArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['nonce', getU64Encoder()], ['head', getU32Encoder()], ['tombstoneHead', getU32Encoder()], ['freeHead', getU32Encoder()], ['activeCount', getU32Encoder()], ['outstandingVaultDepositors', getU32Encoder()], ['feeConfig', getFeeConfigEncoder()], ['takerFee', getU16Encoder()], ['defaultMinLeaderShare', getU32Encoder()], ['baseMint', getAddressEncoder()], ['quoteMint', getAddressEncoder()], ['baseTreasury', getAddressEncoder()], ['quoteTreasury', getAddressEncoder()], ['bump', getU8Encoder()], ['baseTreasuryBump', getU8Encoder()], ['quoteTreasuryBump', getU8Encoder()]]), (value) => ({ ...value, discriminator: MARKET_HEADER_DISCRIMINATOR }));
}

/** Gets the decoder for {@link MarketHeader} account data. */
export function getMarketHeaderDecoder(): FixedSizeDecoder<MarketHeader> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['nonce', getU64Decoder()], ['head', getU32Decoder()], ['tombstoneHead', getU32Decoder()], ['freeHead', getU32Decoder()], ['activeCount', getU32Decoder()], ['outstandingVaultDepositors', getU32Decoder()], ['feeConfig', getFeeConfigDecoder()], ['takerFee', getU16Decoder()], ['defaultMinLeaderShare', getU32Decoder()], ['baseMint', getAddressDecoder()], ['quoteMint', getAddressDecoder()], ['baseTreasury', getAddressDecoder()], ['quoteTreasury', getAddressDecoder()], ['bump', getU8Decoder()], ['baseTreasuryBump', getU8Decoder()], ['quoteTreasuryBump', getU8Decoder()]]);
}

/** Gets the codec for {@link MarketHeader} account data. */
export function getMarketHeaderCodec(): FixedSizeCodec<MarketHeaderArgs, MarketHeader> {
    return combineCodec(getMarketHeaderEncoder(), getMarketHeaderDecoder());
}

export function decodeMarketHeader<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<MarketHeader, TAddress>;
export function decodeMarketHeader<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<MarketHeader, TAddress>;
export function decodeMarketHeader<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<MarketHeader, TAddress> | MaybeAccount<MarketHeader, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getMarketHeaderDecoder());
}

export async function fetchMarketHeader<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<MarketHeader, TAddress>> {
  const maybeAccount = await fetchMaybeMarketHeader(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMarketHeader<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<MarketHeader, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMarketHeader(maybeAccount);
}

export async function fetchAllMarketHeader(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<MarketHeader>[]> {
  const maybeAccounts = await fetchAllMaybeMarketHeader(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMarketHeader(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<MarketHeader>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMarketHeader(maybeAccount));
}

export function getMarketHeaderSize(): number {
  return 245;
}