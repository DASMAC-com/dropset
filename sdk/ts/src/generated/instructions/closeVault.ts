/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { findEventAuthorityPda } from '../pdas';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_VAULT_DISCRIMINATOR = new Uint8Array([14]);

export function getCloseVaultDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(CLOSE_VAULT_DISCRIMINATOR); }

export type CloseVaultInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountSigner extends string | AccountMeta<string> = string, TAccountMarket extends string | AccountMeta<string> = string, TAccountEventAuthority extends string | AccountMeta<string> = string, TAccountProgram extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountSigner extends string ? ReadonlySignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner> : TAccountSigner, TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket, TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority, TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram, ...TRemainingAccounts]>;

export type CloseVaultInstructionData = { discriminator: ReadonlyUint8Array; vaultIdx: number;  };

export type CloseVaultInstructionDataArgs = { vaultIdx: number;  };

export function getCloseVaultInstructionDataEncoder(): FixedSizeEncoder<CloseVaultInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)], ['vaultIdx', getU32Encoder()]]), (value) => ({ ...value, discriminator: CLOSE_VAULT_DISCRIMINATOR }));
}

export function getCloseVaultInstructionDataDecoder(): FixedSizeDecoder<CloseVaultInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)], ['vaultIdx', getU32Decoder()]]);
}

export function getCloseVaultInstructionDataCodec(): FixedSizeCodec<CloseVaultInstructionDataArgs, CloseVaultInstructionData> {
    return combineCodec(getCloseVaultInstructionDataEncoder(), getCloseVaultInstructionDataDecoder());
}

export type CloseVaultAsyncInput<TAccountSigner extends string = string, TAccountMarket extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /** Must equal `vault.leader` — verified in-handler. */
signer: TransactionSigner<TAccountSigner>;
/**
 * Market holding the target vault. `mut` because the active /
 * tombstone list heads and `active_count` change.
 */
market: Address<TAccountMarket>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority?: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
vaultIdx: CloseVaultInstructionDataArgs["vaultIdx"];
}

export async function getCloseVaultInstructionAsync<TAccountSigner extends string, TAccountMarket extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: CloseVaultAsyncInput<TAccountSigner, TAccountMarket, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): Promise<CloseVaultInstruction<TProgramAddress, TAccountSigner, TAccountMarket, TAccountEventAuthority, TAccountProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { signer: { value: input.signer ?? null, isWritable: false }, market: { value: input.market ?? null, isWritable: true }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.eventAuthority.value) {
accounts.eventAuthority.value = await findEventAuthorityPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.signer), getAccountMeta(accounts.market), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getCloseVaultInstructionDataEncoder().encode(args as CloseVaultInstructionDataArgs), programAddress } as CloseVaultInstruction<TProgramAddress, TAccountSigner, TAccountMarket, TAccountEventAuthority, TAccountProgram>);
}

export type CloseVaultInput<TAccountSigner extends string = string, TAccountMarket extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /** Must equal `vault.leader` — verified in-handler. */
signer: TransactionSigner<TAccountSigner>;
/**
 * Market holding the target vault. `mut` because the active /
 * tombstone list heads and `active_count` change.
 */
market: Address<TAccountMarket>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
vaultIdx: CloseVaultInstructionDataArgs["vaultIdx"];
}

export function getCloseVaultInstruction<TAccountSigner extends string, TAccountMarket extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: CloseVaultInput<TAccountSigner, TAccountMarket, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): CloseVaultInstruction<TProgramAddress, TAccountSigner, TAccountMarket, TAccountEventAuthority, TAccountProgram> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { signer: { value: input.signer ?? null, isWritable: false }, market: { value: input.market ?? null, isWritable: true }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.signer), getAccountMeta(accounts.market), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getCloseVaultInstructionDataEncoder().encode(args as CloseVaultInstructionDataArgs), programAddress } as CloseVaultInstruction<TProgramAddress, TAccountSigner, TAccountMarket, TAccountEventAuthority, TAccountProgram>);
}

export type ParsedCloseVaultInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/** Must equal `vault.leader` — verified in-handler. */
signer: TAccountMetas[0];
/**
 * Market holding the target vault. `mut` because the active /
 * tombstone list heads and `active_count` change.
 */
market: TAccountMetas[1];
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: TAccountMetas[2];
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: TAccountMetas[3];
};
data: CloseVaultInstructionData; };

export function parseCloseVaultInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedCloseVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { signer: getNextAccount(), market: getNextAccount(), eventAuthority: getNextAccount(), program: getNextAccount() }, data: getCloseVaultInstructionDataDecoder().decode(instruction.data) };
}