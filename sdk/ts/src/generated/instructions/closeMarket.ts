/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { findRegistryPda } from '../pdas';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_MARKET_DISCRIMINATOR = new Uint8Array([19]);

export function getCloseMarketDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(CLOSE_MARKET_DISCRIMINATOR); }

export type CloseMarketInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountAdmin extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountMarket extends string | AccountMeta<string> = string, TAccountBaseTreasury extends string | AccountMeta<string> = string, TAccountQuoteTreasury extends string | AccountMeta<string> = string, TAccountRentRecipient extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAdmin extends string ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin> : TAccountAdmin, TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry, TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket, TAccountBaseTreasury extends string ? ReadonlyAccount<TAccountBaseTreasury> : TAccountBaseTreasury, TAccountQuoteTreasury extends string ? ReadonlyAccount<TAccountQuoteTreasury> : TAccountQuoteTreasury, TAccountRentRecipient extends string ? WritableAccount<TAccountRentRecipient> : TAccountRentRecipient, ...TRemainingAccounts]>;

export type CloseMarketInstructionData = { discriminator: ReadonlyUint8Array;  };

export type CloseMarketInstructionDataArgs = {  };

export function getCloseMarketInstructionDataEncoder(): FixedSizeEncoder<CloseMarketInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)]]), (value) => ({ ...value, discriminator: CLOSE_MARKET_DISCRIMINATOR }));
}

export function getCloseMarketInstructionDataDecoder(): FixedSizeDecoder<CloseMarketInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)]]);
}

export function getCloseMarketInstructionDataCodec(): FixedSizeCodec<CloseMarketInstructionDataArgs, CloseMarketInstructionData> {
    return combineCodec(getCloseMarketInstructionDataEncoder(), getCloseMarketInstructionDataDecoder());
}

export type CloseMarketAsyncInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountMarket extends string = string, TAccountBaseTreasury extends string = string, TAccountQuoteTreasury extends string = string, TAccountRentRecipient extends string = string> =  {
  /** Registry admin — authorized via the registry admin set. */
admin: TransactionSigner<TAccountAdmin>;
/**
 * Singleton registry. `mut` to decrement `market_count` — the
 * witness `close_registry` later checks is zero.
 */
registry?: Address<TAccountRegistry>;
/**
 * The market being closed. `mut` so its lamports can be drained and
 * its discriminator scrubbed by `Slab::close`. Taken bare, matching
 * every other handler; the treasury `address` constraints below
 * reference this market's stored treasury addresses, so a
 * mismatched market is rejected before the close.
 */
market: Address<TAccountMarket>;
/**
 * The base treasury — must already be closed (zero lamports).
 * CHECK: pinned to `market.base_treasury` and required closed.
 */
baseTreasury: Address<TAccountBaseTreasury>;
/**
 * The quote treasury — must already be closed (zero lamports).
 * CHECK: pinned to `market.quote_treasury` and required closed.
 */
quoteTreasury: Address<TAccountQuoteTreasury>;
/**
 * Receives the market account's rent lamports on close.
 * CHECK: rent destination only.
 */
rentRecipient: Address<TAccountRentRecipient>;
}

export async function getCloseMarketInstructionAsync<TAccountAdmin extends string, TAccountRegistry extends string, TAccountMarket extends string, TAccountBaseTreasury extends string, TAccountQuoteTreasury extends string, TAccountRentRecipient extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: CloseMarketAsyncInput<TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountBaseTreasury, TAccountQuoteTreasury, TAccountRentRecipient>, config?: { programAddress?: TProgramAddress } ): Promise<CloseMarketInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountBaseTreasury, TAccountQuoteTreasury, TAccountRentRecipient>> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: true }, market: { value: input.market ?? null, isWritable: true }, baseTreasury: { value: input.baseTreasury ?? null, isWritable: false }, quoteTreasury: { value: input.quoteTreasury ?? null, isWritable: false }, rentRecipient: { value: input.rentRecipient ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.market), getAccountMeta(accounts.baseTreasury), getAccountMeta(accounts.quoteTreasury), getAccountMeta(accounts.rentRecipient)], data: getCloseMarketInstructionDataEncoder().encode({}), programAddress } as CloseMarketInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountBaseTreasury, TAccountQuoteTreasury, TAccountRentRecipient>);
}

export type CloseMarketInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountMarket extends string = string, TAccountBaseTreasury extends string = string, TAccountQuoteTreasury extends string = string, TAccountRentRecipient extends string = string> =  {
  /** Registry admin — authorized via the registry admin set. */
admin: TransactionSigner<TAccountAdmin>;
/**
 * Singleton registry. `mut` to decrement `market_count` — the
 * witness `close_registry` later checks is zero.
 */
registry: Address<TAccountRegistry>;
/**
 * The market being closed. `mut` so its lamports can be drained and
 * its discriminator scrubbed by `Slab::close`. Taken bare, matching
 * every other handler; the treasury `address` constraints below
 * reference this market's stored treasury addresses, so a
 * mismatched market is rejected before the close.
 */
market: Address<TAccountMarket>;
/**
 * The base treasury — must already be closed (zero lamports).
 * CHECK: pinned to `market.base_treasury` and required closed.
 */
baseTreasury: Address<TAccountBaseTreasury>;
/**
 * The quote treasury — must already be closed (zero lamports).
 * CHECK: pinned to `market.quote_treasury` and required closed.
 */
quoteTreasury: Address<TAccountQuoteTreasury>;
/**
 * Receives the market account's rent lamports on close.
 * CHECK: rent destination only.
 */
rentRecipient: Address<TAccountRentRecipient>;
}

export function getCloseMarketInstruction<TAccountAdmin extends string, TAccountRegistry extends string, TAccountMarket extends string, TAccountBaseTreasury extends string, TAccountQuoteTreasury extends string, TAccountRentRecipient extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: CloseMarketInput<TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountBaseTreasury, TAccountQuoteTreasury, TAccountRentRecipient>, config?: { programAddress?: TProgramAddress } ): CloseMarketInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountBaseTreasury, TAccountQuoteTreasury, TAccountRentRecipient> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: true }, market: { value: input.market ?? null, isWritable: true }, baseTreasury: { value: input.baseTreasury ?? null, isWritable: false }, quoteTreasury: { value: input.quoteTreasury ?? null, isWritable: false }, rentRecipient: { value: input.rentRecipient ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.market), getAccountMeta(accounts.baseTreasury), getAccountMeta(accounts.quoteTreasury), getAccountMeta(accounts.rentRecipient)], data: getCloseMarketInstructionDataEncoder().encode({}), programAddress } as CloseMarketInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountBaseTreasury, TAccountQuoteTreasury, TAccountRentRecipient>);
}

export type ParsedCloseMarketInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/** Registry admin — authorized via the registry admin set. */
admin: TAccountMetas[0];
/**
 * Singleton registry. `mut` to decrement `market_count` — the
 * witness `close_registry` later checks is zero.
 */
registry: TAccountMetas[1];
/**
 * The market being closed. `mut` so its lamports can be drained and
 * its discriminator scrubbed by `Slab::close`. Taken bare, matching
 * every other handler; the treasury `address` constraints below
 * reference this market's stored treasury addresses, so a
 * mismatched market is rejected before the close.
 */
market: TAccountMetas[2];
/**
 * The base treasury — must already be closed (zero lamports).
 * CHECK: pinned to `market.base_treasury` and required closed.
 */
baseTreasury: TAccountMetas[3];
/**
 * The quote treasury — must already be closed (zero lamports).
 * CHECK: pinned to `market.quote_treasury` and required closed.
 */
quoteTreasury: TAccountMetas[4];
/**
 * Receives the market account's rent lamports on close.
 * CHECK: rent destination only.
 */
rentRecipient: TAccountMetas[5];
};
data: CloseMarketInstructionData; };

export function parseCloseMarketInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedCloseMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { admin: getNextAccount(), registry: getNextAccount(), market: getNextAccount(), baseTreasury: getNextAccount(), quoteTreasury: getNextAccount(), rentRecipient: getNextAccount() }, data: getCloseMarketInstructionDataDecoder().decode(instruction.data) };
}