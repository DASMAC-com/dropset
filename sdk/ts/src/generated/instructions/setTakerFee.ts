/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { findEventAuthorityPda, findRegistryPda } from '../pdas';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_TAKER_FEE_DISCRIMINATOR = new Uint8Array([24]);

export function getSetTakerFeeDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(SET_TAKER_FEE_DISCRIMINATOR); }

export type SetTakerFeeInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountAdmin extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountMarket extends string | AccountMeta<string> = string, TAccountEventAuthority extends string | AccountMeta<string> = string, TAccountProgram extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAdmin extends string ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin> : TAccountAdmin, TAccountRegistry extends string ? ReadonlyAccount<TAccountRegistry> : TAccountRegistry, TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket, TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority, TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram, ...TRemainingAccounts]>;

export type SetTakerFeeInstructionData = { discriminator: ReadonlyUint8Array; takerFee: number;  };

export type SetTakerFeeInstructionDataArgs = { takerFee: number;  };

export function getSetTakerFeeInstructionDataEncoder(): FixedSizeEncoder<SetTakerFeeInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)], ['takerFee', getU16Encoder()]]), (value) => ({ ...value, discriminator: SET_TAKER_FEE_DISCRIMINATOR }));
}

export function getSetTakerFeeInstructionDataDecoder(): FixedSizeDecoder<SetTakerFeeInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)], ['takerFee', getU16Decoder()]]);
}

export function getSetTakerFeeInstructionDataCodec(): FixedSizeCodec<SetTakerFeeInstructionDataArgs, SetTakerFeeInstructionData> {
    return combineCodec(getSetTakerFeeInstructionDataEncoder(), getSetTakerFeeInstructionDataDecoder());
}

export type SetTakerFeeAsyncInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountMarket extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /** Registry admin — the only signer this lever accepts. */
admin: TransactionSigner<TAccountAdmin>;
/** Singleton registry, read for the admin-membership check. */
registry?: Address<TAccountRegistry>;
/** Market whose `taker_fee` is being retuned. `mut` for the write. */
market: Address<TAccountMarket>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority?: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
takerFee: SetTakerFeeInstructionDataArgs["takerFee"];
}

export async function getSetTakerFeeInstructionAsync<TAccountAdmin extends string, TAccountRegistry extends string, TAccountMarket extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetTakerFeeAsyncInput<TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): Promise<SetTakerFeeInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountEventAuthority, TAccountProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: false }, market: { value: input.market ?? null, isWritable: true }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}
if (!accounts.eventAuthority.value) {
accounts.eventAuthority.value = await findEventAuthorityPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.market), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getSetTakerFeeInstructionDataEncoder().encode(args as SetTakerFeeInstructionDataArgs), programAddress } as SetTakerFeeInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountEventAuthority, TAccountProgram>);
}

export type SetTakerFeeInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountMarket extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /** Registry admin — the only signer this lever accepts. */
admin: TransactionSigner<TAccountAdmin>;
/** Singleton registry, read for the admin-membership check. */
registry: Address<TAccountRegistry>;
/** Market whose `taker_fee` is being retuned. `mut` for the write. */
market: Address<TAccountMarket>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
takerFee: SetTakerFeeInstructionDataArgs["takerFee"];
}

export function getSetTakerFeeInstruction<TAccountAdmin extends string, TAccountRegistry extends string, TAccountMarket extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetTakerFeeInput<TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): SetTakerFeeInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountEventAuthority, TAccountProgram> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: false }, market: { value: input.market ?? null, isWritable: true }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.market), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getSetTakerFeeInstructionDataEncoder().encode(args as SetTakerFeeInstructionDataArgs), programAddress } as SetTakerFeeInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountMarket, TAccountEventAuthority, TAccountProgram>);
}

export type ParsedSetTakerFeeInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/** Registry admin — the only signer this lever accepts. */
admin: TAccountMetas[0];
/** Singleton registry, read for the admin-membership check. */
registry: TAccountMetas[1];
/** Market whose `taker_fee` is being retuned. `mut` for the write. */
market: TAccountMetas[2];
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: TAccountMetas[3];
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: TAccountMetas[4];
};
data: SetTakerFeeInstructionData; };

export function parseSetTakerFeeInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSetTakerFeeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { admin: getNextAccount(), registry: getNextAccount(), market: getNextAccount(), eventAuthority: getNextAccount(), program: getNextAccount() }, data: getSetTakerFeeInstructionDataDecoder().decode(instruction.data) };
}