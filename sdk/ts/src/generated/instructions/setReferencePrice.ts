/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_REFERENCE_PRICE_DISCRIMINATOR = new Uint8Array([5]);

export function getSetReferencePriceDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(SET_REFERENCE_PRICE_DISCRIMINATOR); }

export type SetReferencePriceInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountSigner extends string | AccountMeta<string> = string, TAccountMarket extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountSigner extends string ? ReadonlySignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner> : TAccountSigner, TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket, ...TRemainingAccounts]>;

export type SetReferencePriceInstructionData = { discriminator: ReadonlyUint8Array; vaultIdx: number; priceBits: number; quoteSlot: number;  };

export type SetReferencePriceInstructionDataArgs = { vaultIdx: number; priceBits: number; quoteSlot: number;  };

export function getSetReferencePriceInstructionDataEncoder(): FixedSizeEncoder<SetReferencePriceInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)], ['vaultIdx', getU32Encoder()], ['priceBits', getU32Encoder()], ['quoteSlot', getU32Encoder()]]), (value) => ({ ...value, discriminator: SET_REFERENCE_PRICE_DISCRIMINATOR }));
}

export function getSetReferencePriceInstructionDataDecoder(): FixedSizeDecoder<SetReferencePriceInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)], ['vaultIdx', getU32Decoder()], ['priceBits', getU32Decoder()], ['quoteSlot', getU32Decoder()]]);
}

export function getSetReferencePriceInstructionDataCodec(): FixedSizeCodec<SetReferencePriceInstructionDataArgs, SetReferencePriceInstructionData> {
    return combineCodec(getSetReferencePriceInstructionDataEncoder(), getSetReferencePriceInstructionDataDecoder());
}

export type SetReferencePriceInput<TAccountSigner extends string = string, TAccountMarket extends string = string> =  {
  /**
 * Quote authority — the only signer the spec accepts here. Set at
 * `CreateVault` (defaults to the leader); the kernel checks it
 * against the target vault's `quote_authority`.
 */
signer: TransactionSigner<TAccountSigner>;
/**
 * CHECK: taken unchecked so the handler can borrow the raw account
 * data and drive the shared kernel (a typed `Market` locks the
 * account exclusively and would deny that borrow). The account's
 * discriminator and owner are not re-validated here: the authority
 * check plus runtime program-ownership at the store are the guards,
 * exactly as on the asm fast path this build mirrors.
 */
market: Address<TAccountMarket>;
vaultIdx: SetReferencePriceInstructionDataArgs["vaultIdx"];
priceBits: SetReferencePriceInstructionDataArgs["priceBits"];
quoteSlot: SetReferencePriceInstructionDataArgs["quoteSlot"];
}

export function getSetReferencePriceInstruction<TAccountSigner extends string, TAccountMarket extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetReferencePriceInput<TAccountSigner, TAccountMarket>, config?: { programAddress?: TProgramAddress } ): SetReferencePriceInstruction<TProgramAddress, TAccountSigner, TAccountMarket> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { signer: { value: input.signer ?? null, isWritable: false }, market: { value: input.market ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.signer), getAccountMeta(accounts.market)], data: getSetReferencePriceInstructionDataEncoder().encode(args as SetReferencePriceInstructionDataArgs), programAddress } as SetReferencePriceInstruction<TProgramAddress, TAccountSigner, TAccountMarket>);
}

export type ParsedSetReferencePriceInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * Quote authority — the only signer the spec accepts here. Set at
 * `CreateVault` (defaults to the leader); the kernel checks it
 * against the target vault's `quote_authority`.
 */
signer: TAccountMetas[0];
/**
 * CHECK: taken unchecked so the handler can borrow the raw account
 * data and drive the shared kernel (a typed `Market` locks the
 * account exclusively and would deny that borrow). The account's
 * discriminator and owner are not re-validated here: the authority
 * check plus runtime program-ownership at the store are the guards,
 * exactly as on the asm fast path this build mirrors.
 */
market: TAccountMetas[1];
};
data: SetReferencePriceInstructionData; };

export function parseSetReferencePriceInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSetReferencePriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { signer: getNextAccount(), market: getNextAccount() }, data: getSetReferencePriceInstructionDataDecoder().decode(instruction.data) };
}