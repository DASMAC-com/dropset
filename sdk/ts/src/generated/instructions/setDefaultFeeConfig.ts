/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { findEventAuthorityPda, findRegistryPda } from '../pdas';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_DEFAULT_FEE_CONFIG_DISCRIMINATOR = new Uint8Array([26]);

export function getSetDefaultFeeConfigDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(SET_DEFAULT_FEE_CONFIG_DISCRIMINATOR); }

export type SetDefaultFeeConfigInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountAdmin extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountFeeMint extends string | AccountMeta<string> = string, TAccountFeeTokenProgram extends string | AccountMeta<string> = string, TAccountRegistryFeeTreasury extends string | AccountMeta<string> = string, TAccountAssociatedTokenProgram extends string | AccountMeta<string> = "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TAccountEventAuthority extends string | AccountMeta<string> = string, TAccountProgram extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAdmin extends string ? WritableSignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin> : TAccountAdmin, TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry, TAccountFeeMint extends string ? ReadonlyAccount<TAccountFeeMint> : TAccountFeeMint, TAccountFeeTokenProgram extends string ? ReadonlyAccount<TAccountFeeTokenProgram> : TAccountFeeTokenProgram, TAccountRegistryFeeTreasury extends string ? WritableAccount<TAccountRegistryFeeTreasury> : TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram extends string ? ReadonlyAccount<TAccountAssociatedTokenProgram> : TAccountAssociatedTokenProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority, TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram, ...TRemainingAccounts]>;

export type SetDefaultFeeConfigInstructionData = { discriminator: ReadonlyUint8Array; atoms: bigint;  };

export type SetDefaultFeeConfigInstructionDataArgs = { atoms: number | bigint;  };

export function getSetDefaultFeeConfigInstructionDataEncoder(): FixedSizeEncoder<SetDefaultFeeConfigInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)], ['atoms', getU64Encoder()]]), (value) => ({ ...value, discriminator: SET_DEFAULT_FEE_CONFIG_DISCRIMINATOR }));
}

export function getSetDefaultFeeConfigInstructionDataDecoder(): FixedSizeDecoder<SetDefaultFeeConfigInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)], ['atoms', getU64Decoder()]]);
}

export function getSetDefaultFeeConfigInstructionDataCodec(): FixedSizeCodec<SetDefaultFeeConfigInstructionDataArgs, SetDefaultFeeConfigInstructionData> {
    return combineCodec(getSetDefaultFeeConfigInstructionDataEncoder(), getSetDefaultFeeConfigInstructionDataDecoder());
}

export type SetDefaultFeeConfigAsyncInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountFeeMint extends string = string, TAccountFeeTokenProgram extends string = string, TAccountRegistryFeeTreasury extends string = string, TAccountAssociatedTokenProgram extends string = string, TAccountSystemProgram extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /**
 * Registry admin — the only signer this lever accepts. `mut`
 * because it funds the rent for the new mint's registry fee ATA
 * when `registry_fee_treasury` is created below.
 */
admin: TransactionSigner<TAccountAdmin>;
/**
 * Singleton registry. `mut` to overwrite `default_fee_config`; also
 * read for the admin-membership check and as the authority of the
 * fee ATA created below.
 */
registry?: Address<TAccountRegistry>;
/**
 * New default fee mint. `mint::token_program = fee_token_program`
 * ties it to the supplied program, so the stored
 * `(mint, token_program)` pair is always a mint backed by its real
 * owner — the check the spec states as `token_program == mint.owner`.
 * `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
 */
feeMint: Address<TAccountFeeMint>;
/**
 * Token program owning `fee_mint` — SPL Token or Token-2022.
 * `Interface<TokenInterface>` rejects any non-token-program address
 * up front; the `mint::token_program` constraint above then pins it
 * to `fee_mint`'s actual owner.
 */
feeTokenProgram: Address<TAccountFeeTokenProgram>;
/**
 * Registry's fee ATA for the new default mint. `create_market` loads
 * the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
 * not `init`) but never creates it, so we create it here, eagerly, at
 * config time — `init_if_needed` so re-pointing the default back to a
 * mint whose ATA already exists is a no-op. The ATA program's
 * `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
 * outright, a stronger backstop than the `mint::token_program`
 * constraint above. Without this, switching the default to a fresh
 * mint would brick the next `create_market` against it until the ATA
 * was created out-of-band — the same hazard
 * `SetMarketFeeConfig` guards at the market level (architecture spec
 * § SetDefaultFeeConfig).
 */
registryFeeTreasury: Address<TAccountRegistryFeeTreasury>;
associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
systemProgram?: Address<TAccountSystemProgram>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority?: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
atoms: SetDefaultFeeConfigInstructionDataArgs["atoms"];
}

export async function getSetDefaultFeeConfigInstructionAsync<TAccountAdmin extends string, TAccountRegistry extends string, TAccountFeeMint extends string, TAccountFeeTokenProgram extends string, TAccountRegistryFeeTreasury extends string, TAccountAssociatedTokenProgram extends string, TAccountSystemProgram extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetDefaultFeeConfigAsyncInput<TAccountAdmin, TAccountRegistry, TAccountFeeMint, TAccountFeeTokenProgram, TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram, TAccountSystemProgram, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): Promise<SetDefaultFeeConfigInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountFeeMint, TAccountFeeTokenProgram, TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram, TAccountSystemProgram, TAccountEventAuthority, TAccountProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: true }, registry: { value: input.registry ?? null, isWritable: true }, feeMint: { value: input.feeMint ?? null, isWritable: false }, feeTokenProgram: { value: input.feeTokenProgram ?? null, isWritable: false }, registryFeeTreasury: { value: input.registryFeeTreasury ?? null, isWritable: true }, associatedTokenProgram: { value: input.associatedTokenProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}
if (!accounts.associatedTokenProgram.value) {
accounts.associatedTokenProgram.value = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL' as Address<'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}
if (!accounts.eventAuthority.value) {
accounts.eventAuthority.value = await findEventAuthorityPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.feeMint), getAccountMeta(accounts.feeTokenProgram), getAccountMeta(accounts.registryFeeTreasury), getAccountMeta(accounts.associatedTokenProgram), getAccountMeta(accounts.systemProgram), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getSetDefaultFeeConfigInstructionDataEncoder().encode(args as SetDefaultFeeConfigInstructionDataArgs), programAddress } as SetDefaultFeeConfigInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountFeeMint, TAccountFeeTokenProgram, TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram, TAccountSystemProgram, TAccountEventAuthority, TAccountProgram>);
}

export type SetDefaultFeeConfigInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountFeeMint extends string = string, TAccountFeeTokenProgram extends string = string, TAccountRegistryFeeTreasury extends string = string, TAccountAssociatedTokenProgram extends string = string, TAccountSystemProgram extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /**
 * Registry admin — the only signer this lever accepts. `mut`
 * because it funds the rent for the new mint's registry fee ATA
 * when `registry_fee_treasury` is created below.
 */
admin: TransactionSigner<TAccountAdmin>;
/**
 * Singleton registry. `mut` to overwrite `default_fee_config`; also
 * read for the admin-membership check and as the authority of the
 * fee ATA created below.
 */
registry: Address<TAccountRegistry>;
/**
 * New default fee mint. `mint::token_program = fee_token_program`
 * ties it to the supplied program, so the stored
 * `(mint, token_program)` pair is always a mint backed by its real
 * owner — the check the spec states as `token_program == mint.owner`.
 * `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
 */
feeMint: Address<TAccountFeeMint>;
/**
 * Token program owning `fee_mint` — SPL Token or Token-2022.
 * `Interface<TokenInterface>` rejects any non-token-program address
 * up front; the `mint::token_program` constraint above then pins it
 * to `fee_mint`'s actual owner.
 */
feeTokenProgram: Address<TAccountFeeTokenProgram>;
/**
 * Registry's fee ATA for the new default mint. `create_market` loads
 * the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
 * not `init`) but never creates it, so we create it here, eagerly, at
 * config time — `init_if_needed` so re-pointing the default back to a
 * mint whose ATA already exists is a no-op. The ATA program's
 * `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
 * outright, a stronger backstop than the `mint::token_program`
 * constraint above. Without this, switching the default to a fresh
 * mint would brick the next `create_market` against it until the ATA
 * was created out-of-band — the same hazard
 * `SetMarketFeeConfig` guards at the market level (architecture spec
 * § SetDefaultFeeConfig).
 */
registryFeeTreasury: Address<TAccountRegistryFeeTreasury>;
associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
systemProgram?: Address<TAccountSystemProgram>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
atoms: SetDefaultFeeConfigInstructionDataArgs["atoms"];
}

export function getSetDefaultFeeConfigInstruction<TAccountAdmin extends string, TAccountRegistry extends string, TAccountFeeMint extends string, TAccountFeeTokenProgram extends string, TAccountRegistryFeeTreasury extends string, TAccountAssociatedTokenProgram extends string, TAccountSystemProgram extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetDefaultFeeConfigInput<TAccountAdmin, TAccountRegistry, TAccountFeeMint, TAccountFeeTokenProgram, TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram, TAccountSystemProgram, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): SetDefaultFeeConfigInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountFeeMint, TAccountFeeTokenProgram, TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram, TAccountSystemProgram, TAccountEventAuthority, TAccountProgram> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: true }, registry: { value: input.registry ?? null, isWritable: true }, feeMint: { value: input.feeMint ?? null, isWritable: false }, feeTokenProgram: { value: input.feeTokenProgram ?? null, isWritable: false }, registryFeeTreasury: { value: input.registryFeeTreasury ?? null, isWritable: true }, associatedTokenProgram: { value: input.associatedTokenProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.associatedTokenProgram.value) {
accounts.associatedTokenProgram.value = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL' as Address<'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.feeMint), getAccountMeta(accounts.feeTokenProgram), getAccountMeta(accounts.registryFeeTreasury), getAccountMeta(accounts.associatedTokenProgram), getAccountMeta(accounts.systemProgram), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getSetDefaultFeeConfigInstructionDataEncoder().encode(args as SetDefaultFeeConfigInstructionDataArgs), programAddress } as SetDefaultFeeConfigInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountFeeMint, TAccountFeeTokenProgram, TAccountRegistryFeeTreasury, TAccountAssociatedTokenProgram, TAccountSystemProgram, TAccountEventAuthority, TAccountProgram>);
}

export type ParsedSetDefaultFeeConfigInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * Registry admin — the only signer this lever accepts. `mut`
 * because it funds the rent for the new mint's registry fee ATA
 * when `registry_fee_treasury` is created below.
 */
admin: TAccountMetas[0];
/**
 * Singleton registry. `mut` to overwrite `default_fee_config`; also
 * read for the admin-membership check and as the authority of the
 * fee ATA created below.
 */
registry: TAccountMetas[1];
/**
 * New default fee mint. `mint::token_program = fee_token_program`
 * ties it to the supplied program, so the stored
 * `(mint, token_program)` pair is always a mint backed by its real
 * owner — the check the spec states as `token_program == mint.owner`.
 * `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
 */
feeMint: TAccountMetas[2];
/**
 * Token program owning `fee_mint` — SPL Token or Token-2022.
 * `Interface<TokenInterface>` rejects any non-token-program address
 * up front; the `mint::token_program` constraint above then pins it
 * to `fee_mint`'s actual owner.
 */
feeTokenProgram: TAccountMetas[3];
/**
 * Registry's fee ATA for the new default mint. `create_market` loads
 * the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
 * not `init`) but never creates it, so we create it here, eagerly, at
 * config time — `init_if_needed` so re-pointing the default back to a
 * mint whose ATA already exists is a no-op. The ATA program's
 * `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
 * outright, a stronger backstop than the `mint::token_program`
 * constraint above. Without this, switching the default to a fresh
 * mint would brick the next `create_market` against it until the ATA
 * was created out-of-band — the same hazard
 * `SetMarketFeeConfig` guards at the market level (architecture spec
 * § SetDefaultFeeConfig).
 */
registryFeeTreasury: TAccountMetas[4];
associatedTokenProgram: TAccountMetas[5];
systemProgram: TAccountMetas[6];
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: TAccountMetas[7];
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: TAccountMetas[8];
};
data: SetDefaultFeeConfigInstructionData; };

export function parseSetDefaultFeeConfigInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSetDefaultFeeConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { admin: getNextAccount(), registry: getNextAccount(), feeMint: getNextAccount(), feeTokenProgram: getNextAccount(), registryFeeTreasury: getNextAccount(), associatedTokenProgram: getNextAccount(), systemProgram: getNextAccount(), eventAuthority: getNextAccount(), program: getNextAccount() }, data: getSetDefaultFeeConfigInstructionDataDecoder().decode(instruction.data) };
}