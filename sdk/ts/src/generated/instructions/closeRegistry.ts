/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { findRegistryPda } from '../pdas';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_REGISTRY_DISCRIMINATOR = new Uint8Array([21]);

export function getCloseRegistryDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(CLOSE_REGISTRY_DISCRIMINATOR); }

export type CloseRegistryInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountAdmin extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountRentRecipient extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAdmin extends string ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin> : TAccountAdmin, TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry, TAccountRentRecipient extends string ? WritableAccount<TAccountRentRecipient> : TAccountRentRecipient, ...TRemainingAccounts]>;

export type CloseRegistryInstructionData = { discriminator: ReadonlyUint8Array;  };

export type CloseRegistryInstructionDataArgs = {  };

export function getCloseRegistryInstructionDataEncoder(): FixedSizeEncoder<CloseRegistryInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)]]), (value) => ({ ...value, discriminator: CLOSE_REGISTRY_DISCRIMINATOR }));
}

export function getCloseRegistryInstructionDataDecoder(): FixedSizeDecoder<CloseRegistryInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)]]);
}

export function getCloseRegistryInstructionDataCodec(): FixedSizeCodec<CloseRegistryInstructionDataArgs, CloseRegistryInstructionData> {
    return combineCodec(getCloseRegistryInstructionDataEncoder(), getCloseRegistryInstructionDataDecoder());
}

export type CloseRegistryAsyncInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountRentRecipient extends string = string> =  {
  /**
 * The last remaining registry admin — the only signer this accepts,
 * and the one whose authority the close is performed under.
 */
admin: TransactionSigner<TAccountAdmin>;
/**
 * The registry being closed. `mut` so its lamports can be drained
 * and its discriminator scrubbed by `Slab::close`.
 */
registry?: Address<TAccountRegistry>;
/**
 * Receives the registry account's rent lamports on close.
 * CHECK: rent destination only.
 */
rentRecipient: Address<TAccountRentRecipient>;
}

export async function getCloseRegistryInstructionAsync<TAccountAdmin extends string, TAccountRegistry extends string, TAccountRentRecipient extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: CloseRegistryAsyncInput<TAccountAdmin, TAccountRegistry, TAccountRentRecipient>, config?: { programAddress?: TProgramAddress } ): Promise<CloseRegistryInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountRentRecipient>> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: true }, rentRecipient: { value: input.rentRecipient ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.rentRecipient)], data: getCloseRegistryInstructionDataEncoder().encode({}), programAddress } as CloseRegistryInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountRentRecipient>);
}

export type CloseRegistryInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountRentRecipient extends string = string> =  {
  /**
 * The last remaining registry admin — the only signer this accepts,
 * and the one whose authority the close is performed under.
 */
admin: TransactionSigner<TAccountAdmin>;
/**
 * The registry being closed. `mut` so its lamports can be drained
 * and its discriminator scrubbed by `Slab::close`.
 */
registry: Address<TAccountRegistry>;
/**
 * Receives the registry account's rent lamports on close.
 * CHECK: rent destination only.
 */
rentRecipient: Address<TAccountRentRecipient>;
}

export function getCloseRegistryInstruction<TAccountAdmin extends string, TAccountRegistry extends string, TAccountRentRecipient extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: CloseRegistryInput<TAccountAdmin, TAccountRegistry, TAccountRentRecipient>, config?: { programAddress?: TProgramAddress } ): CloseRegistryInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountRentRecipient> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: true }, rentRecipient: { value: input.rentRecipient ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.rentRecipient)], data: getCloseRegistryInstructionDataEncoder().encode({}), programAddress } as CloseRegistryInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountRentRecipient>);
}

export type ParsedCloseRegistryInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * The last remaining registry admin — the only signer this accepts,
 * and the one whose authority the close is performed under.
 */
admin: TAccountMetas[0];
/**
 * The registry being closed. `mut` so its lamports can be drained
 * and its discriminator scrubbed by `Slab::close`.
 */
registry: TAccountMetas[1];
/**
 * Receives the registry account's rent lamports on close.
 * CHECK: rent destination only.
 */
rentRecipient: TAccountMetas[2];
};
data: CloseRegistryInstructionData; };

export function parseCloseRegistryInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedCloseRegistryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { admin: getNextAccount(), registry: getNextAccount(), rentRecipient: getNextAccount() }, data: getCloseRegistryInstructionDataDecoder().decode(instruction.data) };
}