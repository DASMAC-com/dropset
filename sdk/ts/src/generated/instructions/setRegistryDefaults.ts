/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getOptionDecoder, getOptionEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, getU32Decoder, getU32Encoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type Codec, type Decoder, type Encoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type Option, type OptionOrNullable, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { findEventAuthorityPda, findRegistryPda } from '../pdas';
import { DROPSET_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_REGISTRY_DEFAULTS_DISCRIMINATOR = new Uint8Array([25]);

export function getSetRegistryDefaultsDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 1).encode(SET_REGISTRY_DEFAULTS_DISCRIMINATOR); }

export type SetRegistryDefaultsInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountAdmin extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountEventAuthority extends string | AccountMeta<string> = string, TAccountProgram extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAdmin extends string ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin> : TAccountAdmin, TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry, TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority, TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram, ...TRemainingAccounts]>;

export type SetRegistryDefaultsInstructionData = { discriminator: ReadonlyUint8Array; takerFee: Option<number>; minLeaderShare: Option<number>;  };

export type SetRegistryDefaultsInstructionDataArgs = { takerFee: OptionOrNullable<number>; minLeaderShare: OptionOrNullable<number>;  };

export function getSetRegistryDefaultsInstructionDataEncoder(): Encoder<SetRegistryDefaultsInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 1)], ['takerFee', getOptionEncoder(getU16Encoder())], ['minLeaderShare', getOptionEncoder(getU32Encoder())]]), (value) => ({ ...value, discriminator: SET_REGISTRY_DEFAULTS_DISCRIMINATOR }));
}

export function getSetRegistryDefaultsInstructionDataDecoder(): Decoder<SetRegistryDefaultsInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 1)], ['takerFee', getOptionDecoder(getU16Decoder())], ['minLeaderShare', getOptionDecoder(getU32Decoder())]]);
}

export function getSetRegistryDefaultsInstructionDataCodec(): Codec<SetRegistryDefaultsInstructionDataArgs, SetRegistryDefaultsInstructionData> {
    return combineCodec(getSetRegistryDefaultsInstructionDataEncoder(), getSetRegistryDefaultsInstructionDataDecoder());
}

export type SetRegistryDefaultsAsyncInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /** Registry admin — the only signer this lever accepts. */
admin: TransactionSigner<TAccountAdmin>;
/**
 * Singleton registry. `mut` for the default writes; also read for
 * the admin-membership check.
 */
registry?: Address<TAccountRegistry>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority?: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
takerFee: SetRegistryDefaultsInstructionDataArgs["takerFee"];
minLeaderShare: SetRegistryDefaultsInstructionDataArgs["minLeaderShare"];
}

export async function getSetRegistryDefaultsInstructionAsync<TAccountAdmin extends string, TAccountRegistry extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetRegistryDefaultsAsyncInput<TAccountAdmin, TAccountRegistry, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): Promise<SetRegistryDefaultsInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountEventAuthority, TAccountProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: true }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}
if (!accounts.eventAuthority.value) {
accounts.eventAuthority.value = await findEventAuthorityPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getSetRegistryDefaultsInstructionDataEncoder().encode(args as SetRegistryDefaultsInstructionDataArgs), programAddress } as SetRegistryDefaultsInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountEventAuthority, TAccountProgram>);
}

export type SetRegistryDefaultsInput<TAccountAdmin extends string = string, TAccountRegistry extends string = string, TAccountEventAuthority extends string = string, TAccountProgram extends string = string> =  {
  /** Registry admin — the only signer this lever accepts. */
admin: TransactionSigner<TAccountAdmin>;
/**
 * Singleton registry. `mut` for the default writes; also read for
 * the admin-membership check.
 */
registry: Address<TAccountRegistry>;
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: Address<TAccountEventAuthority>;
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: Address<TAccountProgram>;
takerFee: SetRegistryDefaultsInstructionDataArgs["takerFee"];
minLeaderShare: SetRegistryDefaultsInstructionDataArgs["minLeaderShare"];
}

export function getSetRegistryDefaultsInstruction<TAccountAdmin extends string, TAccountRegistry extends string, TAccountEventAuthority extends string, TAccountProgram extends string, TProgramAddress extends Address = typeof DROPSET_PROGRAM_ADDRESS>(input: SetRegistryDefaultsInput<TAccountAdmin, TAccountRegistry, TAccountEventAuthority, TAccountProgram>, config?: { programAddress?: TProgramAddress } ): SetRegistryDefaultsInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountEventAuthority, TAccountProgram> {
  // Program address.
const programAddress = config?.programAddress ?? DROPSET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { admin: { value: input.admin ?? null, isWritable: false }, registry: { value: input.registry ?? null, isWritable: true }, eventAuthority: { value: input.eventAuthority ?? null, isWritable: false }, program: { value: input.program ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.registry), getAccountMeta(accounts.eventAuthority), getAccountMeta(accounts.program)], data: getSetRegistryDefaultsInstructionDataEncoder().encode(args as SetRegistryDefaultsInstructionDataArgs), programAddress } as SetRegistryDefaultsInstruction<TProgramAddress, TAccountAdmin, TAccountRegistry, TAccountEventAuthority, TAccountProgram>);
}

export type ParsedSetRegistryDefaultsInstruction<TProgram extends string = typeof DROPSET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/** Registry admin — the only signer this lever accepts. */
admin: TAccountMetas[0];
/**
 * Singleton registry. `mut` for the default writes; also read for
 * the admin-membership check.
 */
registry: TAccountMetas[1];
/** CHECK: Only the event authority can invoke self-CPI */
eventAuthority: TAccountMetas[2];
/** CHECK: Kept for v1-compatible account ordering and IDL shape */
program: TAccountMetas[3];
};
data: SetRegistryDefaultsInstructionData; };

export function parseSetRegistryDefaultsInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSetRegistryDefaultsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { admin: getNextAccount(), registry: getNextAccount(), eventAuthority: getNextAccount(), program: getNextAccount() }, data: getSetRegistryDefaultsInstructionDataDecoder().decode(instruction.data) };
}