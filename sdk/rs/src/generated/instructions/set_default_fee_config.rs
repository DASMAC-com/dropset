//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SET_DEFAULT_FEE_CONFIG_DISCRIMINATOR: [u8; 1] = [26];

/// Accounts.
#[derive(Debug)]
pub struct SetDefaultFeeConfig {
    /// Registry admin — the only signer this lever accepts. `mut`
    /// because it funds the rent for the new mint's registry fee ATA
    /// when `registry_fee_treasury` is created below.
    pub admin: solana_pubkey::Pubkey,
    /// Singleton registry. `mut` to overwrite `default_fee_config`; also
    /// read for the admin-membership check and as the authority of the
    /// fee ATA created below.
    pub registry: solana_pubkey::Pubkey,
    /// New default fee mint. `mint::token_program = fee_token_program`
    /// ties it to the supplied program, so the stored
    /// `(mint, token_program)` pair is always a mint backed by its real
    /// owner — the check the spec states as `token_program == mint.owner`.
    /// `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
    pub fee_mint: solana_pubkey::Pubkey,
    /// Token program owning `fee_mint` — SPL Token or Token-2022.
    /// `Interface<TokenInterface>` rejects any non-token-program address
    /// up front; the `mint::token_program` constraint above then pins it
    /// to `fee_mint`'s actual owner.
    pub fee_token_program: solana_pubkey::Pubkey,
    /// Registry's fee ATA for the new default mint. `create_market` loads
    /// the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
    /// not `init`) but never creates it, so we create it here, eagerly, at
    /// config time — `init_if_needed` so re-pointing the default back to a
    /// mint whose ATA already exists is a no-op. The ATA program's
    /// `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
    /// outright, a stronger backstop than the `mint::token_program`
    /// constraint above. Without this, switching the default to a fresh
    /// mint would brick the next `create_market` against it until the ATA
    /// was created out-of-band — the same ENG-508 hazard
    /// `SetMarketFeeConfig` guards at the market level (architecture spec
    /// § SetDefaultFeeConfig).
    pub registry_fee_treasury: solana_pubkey::Pubkey,

    pub associated_token_program: solana_pubkey::Pubkey,

    pub system_program: solana_pubkey::Pubkey,
    /// CHECK: Only the event authority can invoke self-CPI
    pub event_authority: solana_pubkey::Pubkey,
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    pub program: solana_pubkey::Pubkey,
}

impl SetDefaultFeeConfig {
    pub fn instruction(
        &self,
        args: SetDefaultFeeConfigInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetDefaultFeeConfigInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(9 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.push(solana_instruction::AccountMeta::new(self.registry, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.fee_mint,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.fee_token_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.registry_fee_treasury,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.associated_token_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetDefaultFeeConfigInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::DROPSET_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SetDefaultFeeConfigInstructionData {
    discriminator: [u8; 1],
}

impl SetDefaultFeeConfigInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [26],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetDefaultFeeConfigInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SetDefaultFeeConfigInstructionArgs {
    pub atoms: u64,
}

impl SetDefaultFeeConfigInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetDefaultFeeConfig`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[writable]` registry
///   2. `[]` fee_mint
///   3. `[]` fee_token_program
///   4. `[writable]` registry_fee_treasury
///   5. `[optional]` associated_token_program (default to `ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL`)
///   6. `[optional]` system_program (default to `11111111111111111111111111111111`)
///   7. `[]` event_authority
///   8. `[]` program
#[derive(Clone, Debug, Default)]
pub struct SetDefaultFeeConfigBuilder {
    admin: Option<solana_pubkey::Pubkey>,
    registry: Option<solana_pubkey::Pubkey>,
    fee_mint: Option<solana_pubkey::Pubkey>,
    fee_token_program: Option<solana_pubkey::Pubkey>,
    registry_fee_treasury: Option<solana_pubkey::Pubkey>,
    associated_token_program: Option<solana_pubkey::Pubkey>,
    system_program: Option<solana_pubkey::Pubkey>,
    event_authority: Option<solana_pubkey::Pubkey>,
    program: Option<solana_pubkey::Pubkey>,
    atoms: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetDefaultFeeConfigBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Registry admin — the only signer this lever accepts. `mut`
    /// because it funds the rent for the new mint's registry fee ATA
    /// when `registry_fee_treasury` is created below.
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_pubkey::Pubkey) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    /// Singleton registry. `mut` to overwrite `default_fee_config`; also
    /// read for the admin-membership check and as the authority of the
    /// fee ATA created below.
    #[inline(always)]
    pub fn registry(&mut self, registry: solana_pubkey::Pubkey) -> &mut Self {
        self.registry = Some(registry);
        self
    }
    /// New default fee mint. `mint::token_program = fee_token_program`
    /// ties it to the supplied program, so the stored
    /// `(mint, token_program)` pair is always a mint backed by its real
    /// owner — the check the spec states as `token_program == mint.owner`.
    /// `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
    #[inline(always)]
    pub fn fee_mint(&mut self, fee_mint: solana_pubkey::Pubkey) -> &mut Self {
        self.fee_mint = Some(fee_mint);
        self
    }
    /// Token program owning `fee_mint` — SPL Token or Token-2022.
    /// `Interface<TokenInterface>` rejects any non-token-program address
    /// up front; the `mint::token_program` constraint above then pins it
    /// to `fee_mint`'s actual owner.
    #[inline(always)]
    pub fn fee_token_program(&mut self, fee_token_program: solana_pubkey::Pubkey) -> &mut Self {
        self.fee_token_program = Some(fee_token_program);
        self
    }
    /// Registry's fee ATA for the new default mint. `create_market` loads
    /// the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
    /// not `init`) but never creates it, so we create it here, eagerly, at
    /// config time — `init_if_needed` so re-pointing the default back to a
    /// mint whose ATA already exists is a no-op. The ATA program's
    /// `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
    /// outright, a stronger backstop than the `mint::token_program`
    /// constraint above. Without this, switching the default to a fresh
    /// mint would brick the next `create_market` against it until the ATA
    /// was created out-of-band — the same ENG-508 hazard
    /// `SetMarketFeeConfig` guards at the market level (architecture spec
    /// § SetDefaultFeeConfig).
    #[inline(always)]
    pub fn registry_fee_treasury(
        &mut self,
        registry_fee_treasury: solana_pubkey::Pubkey,
    ) -> &mut Self {
        self.registry_fee_treasury = Some(registry_fee_treasury);
        self
    }
    /// `[optional account, default to 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL']`
    #[inline(always)]
    pub fn associated_token_program(
        &mut self,
        associated_token_program: solana_pubkey::Pubkey,
    ) -> &mut Self {
        self.associated_token_program = Some(associated_token_program);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// CHECK: Only the event authority can invoke self-CPI
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_pubkey::Pubkey) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    #[inline(always)]
    pub fn program(&mut self, program: solana_pubkey::Pubkey) -> &mut Self {
        self.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn atoms(&mut self, atoms: u64) -> &mut Self {
        self.atoms = Some(atoms);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetDefaultFeeConfig {
            admin: self.admin.expect("admin is not set"),
            registry: self.registry.expect("registry is not set"),
            fee_mint: self.fee_mint.expect("fee_mint is not set"),
            fee_token_program: self
                .fee_token_program
                .expect("fee_token_program is not set"),
            registry_fee_treasury: self
                .registry_fee_treasury
                .expect("registry_fee_treasury is not set"),
            associated_token_program: self.associated_token_program.unwrap_or(
                solana_pubkey::pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
            ),
            system_program: self
                .system_program
                .unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
            event_authority: self.event_authority.expect("event_authority is not set"),
            program: self.program.expect("program is not set"),
        };
        let args = SetDefaultFeeConfigInstructionArgs {
            atoms: self.atoms.clone().expect("atoms is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_default_fee_config` CPI accounts.
pub struct SetDefaultFeeConfigCpiAccounts<'a, 'b> {
    /// Registry admin — the only signer this lever accepts. `mut`
    /// because it funds the rent for the new mint's registry fee ATA
    /// when `registry_fee_treasury` is created below.
    pub admin: &'b solana_account_info::AccountInfo<'a>,
    /// Singleton registry. `mut` to overwrite `default_fee_config`; also
    /// read for the admin-membership check and as the authority of the
    /// fee ATA created below.
    pub registry: &'b solana_account_info::AccountInfo<'a>,
    /// New default fee mint. `mint::token_program = fee_token_program`
    /// ties it to the supplied program, so the stored
    /// `(mint, token_program)` pair is always a mint backed by its real
    /// owner — the check the spec states as `token_program == mint.owner`.
    /// `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
    pub fee_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Token program owning `fee_mint` — SPL Token or Token-2022.
    /// `Interface<TokenInterface>` rejects any non-token-program address
    /// up front; the `mint::token_program` constraint above then pins it
    /// to `fee_mint`'s actual owner.
    pub fee_token_program: &'b solana_account_info::AccountInfo<'a>,
    /// Registry's fee ATA for the new default mint. `create_market` loads
    /// the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
    /// not `init`) but never creates it, so we create it here, eagerly, at
    /// config time — `init_if_needed` so re-pointing the default back to a
    /// mint whose ATA already exists is a no-op. The ATA program's
    /// `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
    /// outright, a stronger backstop than the `mint::token_program`
    /// constraint above. Without this, switching the default to a fresh
    /// mint would brick the next `create_market` against it until the ATA
    /// was created out-of-band — the same ENG-508 hazard
    /// `SetMarketFeeConfig` guards at the market level (architecture spec
    /// § SetDefaultFeeConfig).
    pub registry_fee_treasury: &'b solana_account_info::AccountInfo<'a>,

    pub associated_token_program: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Only the event authority can invoke self-CPI
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    pub program: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_default_fee_config` CPI instruction.
pub struct SetDefaultFeeConfigCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Registry admin — the only signer this lever accepts. `mut`
    /// because it funds the rent for the new mint's registry fee ATA
    /// when `registry_fee_treasury` is created below.
    pub admin: &'b solana_account_info::AccountInfo<'a>,
    /// Singleton registry. `mut` to overwrite `default_fee_config`; also
    /// read for the admin-membership check and as the authority of the
    /// fee ATA created below.
    pub registry: &'b solana_account_info::AccountInfo<'a>,
    /// New default fee mint. `mint::token_program = fee_token_program`
    /// ties it to the supplied program, so the stored
    /// `(mint, token_program)` pair is always a mint backed by its real
    /// owner — the check the spec states as `token_program == mint.owner`.
    /// `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
    pub fee_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Token program owning `fee_mint` — SPL Token or Token-2022.
    /// `Interface<TokenInterface>` rejects any non-token-program address
    /// up front; the `mint::token_program` constraint above then pins it
    /// to `fee_mint`'s actual owner.
    pub fee_token_program: &'b solana_account_info::AccountInfo<'a>,
    /// Registry's fee ATA for the new default mint. `create_market` loads
    /// the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
    /// not `init`) but never creates it, so we create it here, eagerly, at
    /// config time — `init_if_needed` so re-pointing the default back to a
    /// mint whose ATA already exists is a no-op. The ATA program's
    /// `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
    /// outright, a stronger backstop than the `mint::token_program`
    /// constraint above. Without this, switching the default to a fresh
    /// mint would brick the next `create_market` against it until the ATA
    /// was created out-of-band — the same ENG-508 hazard
    /// `SetMarketFeeConfig` guards at the market level (architecture spec
    /// § SetDefaultFeeConfig).
    pub registry_fee_treasury: &'b solana_account_info::AccountInfo<'a>,

    pub associated_token_program: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Only the event authority can invoke self-CPI
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    pub program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SetDefaultFeeConfigInstructionArgs,
}

impl<'a, 'b> SetDefaultFeeConfigCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetDefaultFeeConfigCpiAccounts<'a, 'b>,
        args: SetDefaultFeeConfigInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            admin: accounts.admin,
            registry: accounts.registry,
            fee_mint: accounts.fee_mint,
            fee_token_program: accounts.fee_token_program,
            registry_fee_treasury: accounts.registry_fee_treasury,
            associated_token_program: accounts.associated_token_program,
            system_program: accounts.system_program,
            event_authority: accounts.event_authority,
            program: accounts.program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(9 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.registry.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.fee_mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.fee_token_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.registry_fee_treasury.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.associated_token_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = SetDefaultFeeConfigInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::DROPSET_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(10 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.registry.clone());
        account_infos.push(self.fee_mint.clone());
        account_infos.push(self.fee_token_program.clone());
        account_infos.push(self.registry_fee_treasury.clone());
        account_infos.push(self.associated_token_program.clone());
        account_infos.push(self.system_program.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetDefaultFeeConfig` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[writable]` registry
///   2. `[]` fee_mint
///   3. `[]` fee_token_program
///   4. `[writable]` registry_fee_treasury
///   5. `[]` associated_token_program
///   6. `[]` system_program
///   7. `[]` event_authority
///   8. `[]` program
#[derive(Clone, Debug)]
pub struct SetDefaultFeeConfigCpiBuilder<'a, 'b> {
    instruction: Box<SetDefaultFeeConfigCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetDefaultFeeConfigCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetDefaultFeeConfigCpiBuilderInstruction {
            __program: program,
            admin: None,
            registry: None,
            fee_mint: None,
            fee_token_program: None,
            registry_fee_treasury: None,
            associated_token_program: None,
            system_program: None,
            event_authority: None,
            program: None,
            atoms: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Registry admin — the only signer this lever accepts. `mut`
    /// because it funds the rent for the new mint's registry fee ATA
    /// when `registry_fee_treasury` is created below.
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    /// Singleton registry. `mut` to overwrite `default_fee_config`; also
    /// read for the admin-membership check and as the authority of the
    /// fee ATA created below.
    #[inline(always)]
    pub fn registry(&mut self, registry: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.registry = Some(registry);
        self
    }
    /// New default fee mint. `mint::token_program = fee_token_program`
    /// ties it to the supplied program, so the stored
    /// `(mint, token_program)` pair is always a mint backed by its real
    /// owner — the check the spec states as `token_program == mint.owner`.
    /// `InterfaceAccount<Mint>` additionally rejects a non-mint payload.
    #[inline(always)]
    pub fn fee_mint(&mut self, fee_mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.fee_mint = Some(fee_mint);
        self
    }
    /// Token program owning `fee_mint` — SPL Token or Token-2022.
    /// `Interface<TokenInterface>` rejects any non-token-program address
    /// up front; the `mint::token_program` constraint above then pins it
    /// to `fee_mint`'s actual owner.
    #[inline(always)]
    pub fn fee_token_program(
        &mut self,
        fee_token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.fee_token_program = Some(fee_token_program);
        self
    }
    /// Registry's fee ATA for the new default mint. `create_market` loads
    /// the registry fee ATA for `registry.default_fee_config.mint` (`mut`,
    /// not `init`) but never creates it, so we create it here, eagerly, at
    /// config time — `init_if_needed` so re-pointing the default back to a
    /// mint whose ATA already exists is a no-op. The ATA program's
    /// `InitializeAccount3` CPI rejects a non-mint / wrong-program payload
    /// outright, a stronger backstop than the `mint::token_program`
    /// constraint above. Without this, switching the default to a fresh
    /// mint would brick the next `create_market` against it until the ATA
    /// was created out-of-band — the same ENG-508 hazard
    /// `SetMarketFeeConfig` guards at the market level (architecture spec
    /// § SetDefaultFeeConfig).
    #[inline(always)]
    pub fn registry_fee_treasury(
        &mut self,
        registry_fee_treasury: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.registry_fee_treasury = Some(registry_fee_treasury);
        self
    }
    #[inline(always)]
    pub fn associated_token_program(
        &mut self,
        associated_token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.associated_token_program = Some(associated_token_program);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// CHECK: Only the event authority can invoke self-CPI
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    #[inline(always)]
    pub fn program(&mut self, program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn atoms(&mut self, atoms: u64) -> &mut Self {
        self.instruction.atoms = Some(atoms);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetDefaultFeeConfigInstructionArgs {
            atoms: self.instruction.atoms.clone().expect("atoms is not set"),
        };
        let instruction = SetDefaultFeeConfigCpi {
            __program: self.instruction.__program,

            admin: self.instruction.admin.expect("admin is not set"),

            registry: self.instruction.registry.expect("registry is not set"),

            fee_mint: self.instruction.fee_mint.expect("fee_mint is not set"),

            fee_token_program: self
                .instruction
                .fee_token_program
                .expect("fee_token_program is not set"),

            registry_fee_treasury: self
                .instruction
                .registry_fee_treasury
                .expect("registry_fee_treasury is not set"),

            associated_token_program: self
                .instruction
                .associated_token_program
                .expect("associated_token_program is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            program: self.instruction.program.expect("program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetDefaultFeeConfigCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    registry: Option<&'b solana_account_info::AccountInfo<'a>>,
    fee_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    fee_token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    registry_fee_treasury: Option<&'b solana_account_info::AccountInfo<'a>>,
    associated_token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    program: Option<&'b solana_account_info::AccountInfo<'a>>,
    atoms: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
