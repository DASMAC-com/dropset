//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const SET_REFERENCE_PRICE_DISCRIMINATOR: [u8; 1] = [5];

/// Accounts.
#[derive(Debug)]
pub struct SetReferencePrice {
            /// Quote authority — the only signer the spec accepts here. Set at
/// `OpenVault` (defaults to the leader).

    
              
          pub signer: solana_pubkey::Pubkey,
                /// Market account holding the target vault.

    
              
          pub market: solana_pubkey::Pubkey,
                /// Read for the current slot. The leader-supplied `quote_slot` is
/// validated against this (no future-dating, capped backdate).

    
              
          pub clock: solana_pubkey::Pubkey,
      }

impl SetReferencePrice {
  pub fn instruction(&self, args: SetReferencePriceInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: SetReferencePriceInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.market,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.clock,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = SetReferencePriceInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::DROPSET_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct SetReferencePriceInstructionData {
            discriminator: [u8; 1],
                        }

impl SetReferencePriceInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [5],
                                                            }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for SetReferencePriceInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct SetReferencePriceInstructionArgs {
                  pub vault_idx: u32,
                pub price_bits: u32,
                pub quote_slot: u64,
      }

impl SetReferencePriceInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `SetReferencePrice`.
///
/// ### Accounts:
///
                ///   0. `[signer]` signer
                ///   1. `[writable]` market
                ///   2. `[optional]` clock (default to `SysvarC1ock11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct SetReferencePriceBuilder {
            signer: Option<solana_pubkey::Pubkey>,
                market: Option<solana_pubkey::Pubkey>,
                clock: Option<solana_pubkey::Pubkey>,
                        vault_idx: Option<u32>,
                price_bits: Option<u32>,
                quote_slot: Option<u64>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetReferencePriceBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            /// Quote authority — the only signer the spec accepts here. Set at
/// `OpenVault` (defaults to the leader).
#[inline(always)]
    pub fn signer(&mut self, signer: solana_pubkey::Pubkey) -> &mut Self {
                        self.signer = Some(signer);
                    self
    }
            /// Market account holding the target vault.
#[inline(always)]
    pub fn market(&mut self, market: solana_pubkey::Pubkey) -> &mut Self {
                        self.market = Some(market);
                    self
    }
            /// `[optional account, default to 'SysvarC1ock11111111111111111111111111111111']`
/// Read for the current slot. The leader-supplied `quote_slot` is
/// validated against this (no future-dating, capped backdate).
#[inline(always)]
    pub fn clock(&mut self, clock: solana_pubkey::Pubkey) -> &mut Self {
                        self.clock = Some(clock);
                    self
    }
                    #[inline(always)]
      pub fn vault_idx(&mut self, vault_idx: u32) -> &mut Self {
        self.vault_idx = Some(vault_idx);
        self
      }
                #[inline(always)]
      pub fn price_bits(&mut self, price_bits: u32) -> &mut Self {
        self.price_bits = Some(price_bits);
        self
      }
                #[inline(always)]
      pub fn quote_slot(&mut self, quote_slot: u64) -> &mut Self {
        self.quote_slot = Some(quote_slot);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = SetReferencePrice {
                              signer: self.signer.expect("signer is not set"),
                                        market: self.market.expect("market is not set"),
                                        clock: self.clock.unwrap_or(solana_pubkey::pubkey!("SysvarC1ock11111111111111111111111111111111")),
                      };
          let args = SetReferencePriceInstructionArgs {
                                                              vault_idx: self.vault_idx.clone().expect("vault_idx is not set"),
                                                                  price_bits: self.price_bits.clone().expect("price_bits is not set"),
                                                                  quote_slot: self.quote_slot.clone().expect("quote_slot is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `set_reference_price` CPI accounts.
  pub struct SetReferencePriceCpiAccounts<'a, 'b> {
                  /// Quote authority — the only signer the spec accepts here. Set at
/// `OpenVault` (defaults to the leader).

      
                    
              pub signer: &'b solana_account_info::AccountInfo<'a>,
                        /// Market account holding the target vault.

      
                    
              pub market: &'b solana_account_info::AccountInfo<'a>,
                        /// Read for the current slot. The leader-supplied `quote_slot` is
/// validated against this (no future-dating, capped backdate).

      
                    
              pub clock: &'b solana_account_info::AccountInfo<'a>,
            }

/// `set_reference_price` CPI instruction.
pub struct SetReferencePriceCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
            /// Quote authority — the only signer the spec accepts here. Set at
/// `OpenVault` (defaults to the leader).

    
              
          pub signer: &'b solana_account_info::AccountInfo<'a>,
                /// Market account holding the target vault.

    
              
          pub market: &'b solana_account_info::AccountInfo<'a>,
                /// Read for the current slot. The leader-supplied `quote_slot` is
/// validated against this (no future-dating, capped backdate).

    
              
          pub clock: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: SetReferencePriceInstructionArgs,
  }

impl<'a, 'b> SetReferencePriceCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: SetReferencePriceCpiAccounts<'a, 'b>,
              args: SetReferencePriceInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              signer: accounts.signer,
              market: accounts.market,
              clock: accounts.clock,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.clock.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = SetReferencePriceInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::DROPSET_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.signer.clone());
                        account_infos.push(self.market.clone());
                        account_infos.push(self.clock.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `SetReferencePrice` via CPI.
///
/// ### Accounts:
///
                ///   0. `[signer]` signer
                ///   1. `[writable]` market
          ///   2. `[]` clock
#[derive(Clone, Debug)]
pub struct SetReferencePriceCpiBuilder<'a, 'b> {
  instruction: Box<SetReferencePriceCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetReferencePriceCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(SetReferencePriceCpiBuilderInstruction {
      __program: program,
              signer: None,
              market: None,
              clock: None,
                                            vault_idx: None,
                                price_bits: None,
                                quote_slot: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      /// Quote authority — the only signer the spec accepts here. Set at
/// `OpenVault` (defaults to the leader).
#[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.signer = Some(signer);
                    self
    }
      /// Market account holding the target vault.
#[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.market = Some(market);
                    self
    }
      /// Read for the current slot. The leader-supplied `quote_slot` is
/// validated against this (no future-dating, capped backdate).
#[inline(always)]
    pub fn clock(&mut self, clock: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.clock = Some(clock);
                    self
    }
                    #[inline(always)]
      pub fn vault_idx(&mut self, vault_idx: u32) -> &mut Self {
        self.instruction.vault_idx = Some(vault_idx);
        self
      }
                #[inline(always)]
      pub fn price_bits(&mut self, price_bits: u32) -> &mut Self {
        self.instruction.price_bits = Some(price_bits);
        self
      }
                #[inline(always)]
      pub fn quote_slot(&mut self, quote_slot: u64) -> &mut Self {
        self.instruction.quote_slot = Some(quote_slot);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = SetReferencePriceInstructionArgs {
                                                              vault_idx: self.instruction.vault_idx.clone().expect("vault_idx is not set"),
                                                                  price_bits: self.instruction.price_bits.clone().expect("price_bits is not set"),
                                                                  quote_slot: self.instruction.quote_slot.clone().expect("quote_slot is not set"),
                                    };
        let instruction = SetReferencePriceCpi {
        __program: self.instruction.__program,
                  
          signer: self.instruction.signer.expect("signer is not set"),
                  
          market: self.instruction.market.expect("market is not set"),
                  
          clock: self.instruction.clock.expect("clock is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct SetReferencePriceCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            signer: Option<&'b solana_account_info::AccountInfo<'a>>,
                market: Option<&'b solana_account_info::AccountInfo<'a>>,
                clock: Option<&'b solana_account_info::AccountInfo<'a>>,
                        vault_idx: Option<u32>,
                price_bits: Option<u32>,
                quote_slot: Option<u64>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

