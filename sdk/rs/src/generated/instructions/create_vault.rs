//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

pub const CREATE_VAULT_DISCRIMINATOR: [u8; 1] = [4];

/// Accounts.
#[derive(Debug)]
pub struct CreateVault {
    /// Pays sector-rent top-up (if the slab realloc grows the
    /// account) and the create-vault fee (unless waived for an admin).
    /// Becomes the vault's `leader` unless an admin supplied a
    /// distinct `leader_override` — see the handler.
    pub payer: solana_pubkey::Pubkey,
    /// Singleton registry — read for admin check + the
    /// `max_vaults_per_market` cap.
    pub registry: solana_pubkey::Pubkey,
    /// Market the vault lives on. `mut` because the slab tail grows
    /// (or pops from the free list) and `active_count` increments.
    pub market: solana_pubkey::Pubkey,
    /// Mint the create-vault fee is paid in. Pinned to the value
    /// `create_market` stamped into `market.fee_config.mint`.
    pub fee_mint: solana_pubkey::Pubkey,
    /// Token program owning `fee_mint`. Pinned to the value stamped at
    /// market creation.
    pub fee_token_program: solana_pubkey::Pubkey,
    /// Caller's source ATA for the fee mint. Only read on the
    /// non-admin path; admin path skips the transfer entirely.
    pub payer_fee_source: solana_pubkey::Pubkey,
    /// Registry's fee ATA. ATA constraint validates
    /// `(registry, fee_mint, fee_token_program)` so a wrong destination
    /// is rejected before the transfer CPI runs.
    pub registry_fee_treasury: solana_pubkey::Pubkey,

    pub system_program: solana_pubkey::Pubkey,
    /// CHECK: Only the event authority can invoke self-CPI
    pub event_authority: solana_pubkey::Pubkey,
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    pub program: solana_pubkey::Pubkey,
}

impl CreateVault {
    pub fn instruction(&self, args: CreateVaultInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: CreateVaultInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(10 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.registry,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.fee_mint,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.fee_token_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.payer_fee_source,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.registry_fee_treasury,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = CreateVaultInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::DROPSET_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateVaultInstructionData {
    discriminator: [u8; 1],
}

impl CreateVaultInstructionData {
    pub fn new() -> Self {
        Self { discriminator: [4] }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CreateVaultInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateVaultInstructionArgs {
    pub perf_fee_rate: u32,
    pub quote_authority: Pubkey,
    pub allow_outside_depositors: bool,
    pub leader_override: Pubkey,
}

impl CreateVaultInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `CreateVault`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` payer
///   1. `[]` registry
///   2. `[writable]` market
///   3. `[optional]` fee_mint (default to `market.fee_config.mint`)
///   4. `[optional]` fee_token_program (default to `market.fee_config.token_program`)
///   5. `[writable]` payer_fee_source
///   6. `[writable]` registry_fee_treasury
///   7. `[optional]` system_program (default to `11111111111111111111111111111111`)
///   8. `[]` event_authority
///   9. `[]` program
#[derive(Clone, Debug, Default)]
pub struct CreateVaultBuilder {
    payer: Option<solana_pubkey::Pubkey>,
    registry: Option<solana_pubkey::Pubkey>,
    market: Option<solana_pubkey::Pubkey>,
    fee_mint: Option<solana_pubkey::Pubkey>,
    fee_token_program: Option<solana_pubkey::Pubkey>,
    payer_fee_source: Option<solana_pubkey::Pubkey>,
    registry_fee_treasury: Option<solana_pubkey::Pubkey>,
    system_program: Option<solana_pubkey::Pubkey>,
    event_authority: Option<solana_pubkey::Pubkey>,
    program: Option<solana_pubkey::Pubkey>,
    perf_fee_rate: Option<u32>,
    quote_authority: Option<Pubkey>,
    allow_outside_depositors: Option<bool>,
    leader_override: Option<Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CreateVaultBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Pays sector-rent top-up (if the slab realloc grows the
    /// account) and the create-vault fee (unless waived for an admin).
    /// Becomes the vault's `leader` unless an admin supplied a
    /// distinct `leader_override` — see the handler.
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_pubkey::Pubkey) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    /// Singleton registry — read for admin check + the
    /// `max_vaults_per_market` cap.
    #[inline(always)]
    pub fn registry(&mut self, registry: solana_pubkey::Pubkey) -> &mut Self {
        self.registry = Some(registry);
        self
    }
    /// Market the vault lives on. `mut` because the slab tail grows
    /// (or pops from the free list) and `active_count` increments.
    #[inline(always)]
    pub fn market(&mut self, market: solana_pubkey::Pubkey) -> &mut Self {
        self.market = Some(market);
        self
    }
    /// `[optional account, default to 'market.fee_config.mint']`
    /// Mint the create-vault fee is paid in. Pinned to the value
    /// `create_market` stamped into `market.fee_config.mint`.
    #[inline(always)]
    pub fn fee_mint(&mut self, fee_mint: solana_pubkey::Pubkey) -> &mut Self {
        self.fee_mint = Some(fee_mint);
        self
    }
    /// `[optional account, default to 'market.fee_config.token_program']`
    /// Token program owning `fee_mint`. Pinned to the value stamped at
    /// market creation.
    #[inline(always)]
    pub fn fee_token_program(&mut self, fee_token_program: solana_pubkey::Pubkey) -> &mut Self {
        self.fee_token_program = Some(fee_token_program);
        self
    }
    /// Caller's source ATA for the fee mint. Only read on the
    /// non-admin path; admin path skips the transfer entirely.
    #[inline(always)]
    pub fn payer_fee_source(&mut self, payer_fee_source: solana_pubkey::Pubkey) -> &mut Self {
        self.payer_fee_source = Some(payer_fee_source);
        self
    }
    /// Registry's fee ATA. ATA constraint validates
    /// `(registry, fee_mint, fee_token_program)` so a wrong destination
    /// is rejected before the transfer CPI runs.
    #[inline(always)]
    pub fn registry_fee_treasury(
        &mut self,
        registry_fee_treasury: solana_pubkey::Pubkey,
    ) -> &mut Self {
        self.registry_fee_treasury = Some(registry_fee_treasury);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// CHECK: Only the event authority can invoke self-CPI
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_pubkey::Pubkey) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    #[inline(always)]
    pub fn program(&mut self, program: solana_pubkey::Pubkey) -> &mut Self {
        self.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn perf_fee_rate(&mut self, perf_fee_rate: u32) -> &mut Self {
        self.perf_fee_rate = Some(perf_fee_rate);
        self
    }
    #[inline(always)]
    pub fn quote_authority(&mut self, quote_authority: Pubkey) -> &mut Self {
        self.quote_authority = Some(quote_authority);
        self
    }
    #[inline(always)]
    pub fn allow_outside_depositors(&mut self, allow_outside_depositors: bool) -> &mut Self {
        self.allow_outside_depositors = Some(allow_outside_depositors);
        self
    }
    #[inline(always)]
    pub fn leader_override(&mut self, leader_override: Pubkey) -> &mut Self {
        self.leader_override = Some(leader_override);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CreateVault {
            payer: self.payer.expect("payer is not set"),
            registry: self.registry.expect("registry is not set"),
            market: self.market.expect("market is not set"),
            fee_mint: self
                .fee_mint
                .unwrap_or(solana_pubkey::pubkey!("market.fee_config.mint")),
            fee_token_program: self
                .fee_token_program
                .unwrap_or(solana_pubkey::pubkey!("market.fee_config.token_program")),
            payer_fee_source: self.payer_fee_source.expect("payer_fee_source is not set"),
            registry_fee_treasury: self
                .registry_fee_treasury
                .expect("registry_fee_treasury is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_pubkey::pubkey!("11111111111111111111111111111111")),
            event_authority: self.event_authority.expect("event_authority is not set"),
            program: self.program.expect("program is not set"),
        };
        let args = CreateVaultInstructionArgs {
            perf_fee_rate: self
                .perf_fee_rate
                .clone()
                .expect("perf_fee_rate is not set"),
            quote_authority: self
                .quote_authority
                .clone()
                .expect("quote_authority is not set"),
            allow_outside_depositors: self
                .allow_outside_depositors
                .clone()
                .expect("allow_outside_depositors is not set"),
            leader_override: self
                .leader_override
                .clone()
                .expect("leader_override is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `create_vault` CPI accounts.
pub struct CreateVaultCpiAccounts<'a, 'b> {
    /// Pays sector-rent top-up (if the slab realloc grows the
    /// account) and the create-vault fee (unless waived for an admin).
    /// Becomes the vault's `leader` unless an admin supplied a
    /// distinct `leader_override` — see the handler.
    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// Singleton registry — read for admin check + the
    /// `max_vaults_per_market` cap.
    pub registry: &'b solana_account_info::AccountInfo<'a>,
    /// Market the vault lives on. `mut` because the slab tail grows
    /// (or pops from the free list) and `active_count` increments.
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Mint the create-vault fee is paid in. Pinned to the value
    /// `create_market` stamped into `market.fee_config.mint`.
    pub fee_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Token program owning `fee_mint`. Pinned to the value stamped at
    /// market creation.
    pub fee_token_program: &'b solana_account_info::AccountInfo<'a>,
    /// Caller's source ATA for the fee mint. Only read on the
    /// non-admin path; admin path skips the transfer entirely.
    pub payer_fee_source: &'b solana_account_info::AccountInfo<'a>,
    /// Registry's fee ATA. ATA constraint validates
    /// `(registry, fee_mint, fee_token_program)` so a wrong destination
    /// is rejected before the transfer CPI runs.
    pub registry_fee_treasury: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Only the event authority can invoke self-CPI
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    pub program: &'b solana_account_info::AccountInfo<'a>,
}

/// `create_vault` CPI instruction.
pub struct CreateVaultCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Pays sector-rent top-up (if the slab realloc grows the
    /// account) and the create-vault fee (unless waived for an admin).
    /// Becomes the vault's `leader` unless an admin supplied a
    /// distinct `leader_override` — see the handler.
    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// Singleton registry — read for admin check + the
    /// `max_vaults_per_market` cap.
    pub registry: &'b solana_account_info::AccountInfo<'a>,
    /// Market the vault lives on. `mut` because the slab tail grows
    /// (or pops from the free list) and `active_count` increments.
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Mint the create-vault fee is paid in. Pinned to the value
    /// `create_market` stamped into `market.fee_config.mint`.
    pub fee_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Token program owning `fee_mint`. Pinned to the value stamped at
    /// market creation.
    pub fee_token_program: &'b solana_account_info::AccountInfo<'a>,
    /// Caller's source ATA for the fee mint. Only read on the
    /// non-admin path; admin path skips the transfer entirely.
    pub payer_fee_source: &'b solana_account_info::AccountInfo<'a>,
    /// Registry's fee ATA. ATA constraint validates
    /// `(registry, fee_mint, fee_token_program)` so a wrong destination
    /// is rejected before the transfer CPI runs.
    pub registry_fee_treasury: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Only the event authority can invoke self-CPI
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    pub program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: CreateVaultInstructionArgs,
}

impl<'a, 'b> CreateVaultCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CreateVaultCpiAccounts<'a, 'b>,
        args: CreateVaultInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            payer: accounts.payer,
            registry: accounts.registry,
            market: accounts.market,
            fee_mint: accounts.fee_mint,
            fee_token_program: accounts.fee_token_program,
            payer_fee_source: accounts.payer_fee_source,
            registry_fee_treasury: accounts.registry_fee_treasury,
            system_program: accounts.system_program,
            event_authority: accounts.event_authority,
            program: accounts.program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(10 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.registry.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.fee_mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.fee_token_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.payer_fee_source.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.registry_fee_treasury.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = CreateVaultInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::DROPSET_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(11 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.registry.clone());
        account_infos.push(self.market.clone());
        account_infos.push(self.fee_mint.clone());
        account_infos.push(self.fee_token_program.clone());
        account_infos.push(self.payer_fee_source.clone());
        account_infos.push(self.registry_fee_treasury.clone());
        account_infos.push(self.system_program.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CreateVault` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` payer
///   1. `[]` registry
///   2. `[writable]` market
///   3. `[]` fee_mint
///   4. `[]` fee_token_program
///   5. `[writable]` payer_fee_source
///   6. `[writable]` registry_fee_treasury
///   7. `[]` system_program
///   8. `[]` event_authority
///   9. `[]` program
#[derive(Clone, Debug)]
pub struct CreateVaultCpiBuilder<'a, 'b> {
    instruction: Box<CreateVaultCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CreateVaultCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CreateVaultCpiBuilderInstruction {
            __program: program,
            payer: None,
            registry: None,
            market: None,
            fee_mint: None,
            fee_token_program: None,
            payer_fee_source: None,
            registry_fee_treasury: None,
            system_program: None,
            event_authority: None,
            program: None,
            perf_fee_rate: None,
            quote_authority: None,
            allow_outside_depositors: None,
            leader_override: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Pays sector-rent top-up (if the slab realloc grows the
    /// account) and the create-vault fee (unless waived for an admin).
    /// Becomes the vault's `leader` unless an admin supplied a
    /// distinct `leader_override` — see the handler.
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    /// Singleton registry — read for admin check + the
    /// `max_vaults_per_market` cap.
    #[inline(always)]
    pub fn registry(&mut self, registry: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.registry = Some(registry);
        self
    }
    /// Market the vault lives on. `mut` because the slab tail grows
    /// (or pops from the free list) and `active_count` increments.
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    /// Mint the create-vault fee is paid in. Pinned to the value
    /// `create_market` stamped into `market.fee_config.mint`.
    #[inline(always)]
    pub fn fee_mint(&mut self, fee_mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.fee_mint = Some(fee_mint);
        self
    }
    /// Token program owning `fee_mint`. Pinned to the value stamped at
    /// market creation.
    #[inline(always)]
    pub fn fee_token_program(
        &mut self,
        fee_token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.fee_token_program = Some(fee_token_program);
        self
    }
    /// Caller's source ATA for the fee mint. Only read on the
    /// non-admin path; admin path skips the transfer entirely.
    #[inline(always)]
    pub fn payer_fee_source(
        &mut self,
        payer_fee_source: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.payer_fee_source = Some(payer_fee_source);
        self
    }
    /// Registry's fee ATA. ATA constraint validates
    /// `(registry, fee_mint, fee_token_program)` so a wrong destination
    /// is rejected before the transfer CPI runs.
    #[inline(always)]
    pub fn registry_fee_treasury(
        &mut self,
        registry_fee_treasury: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.registry_fee_treasury = Some(registry_fee_treasury);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// CHECK: Only the event authority can invoke self-CPI
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    /// CHECK: Kept for v1-compatible account ordering and IDL shape
    #[inline(always)]
    pub fn program(&mut self, program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn perf_fee_rate(&mut self, perf_fee_rate: u32) -> &mut Self {
        self.instruction.perf_fee_rate = Some(perf_fee_rate);
        self
    }
    #[inline(always)]
    pub fn quote_authority(&mut self, quote_authority: Pubkey) -> &mut Self {
        self.instruction.quote_authority = Some(quote_authority);
        self
    }
    #[inline(always)]
    pub fn allow_outside_depositors(&mut self, allow_outside_depositors: bool) -> &mut Self {
        self.instruction.allow_outside_depositors = Some(allow_outside_depositors);
        self
    }
    #[inline(always)]
    pub fn leader_override(&mut self, leader_override: Pubkey) -> &mut Self {
        self.instruction.leader_override = Some(leader_override);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = CreateVaultInstructionArgs {
            perf_fee_rate: self
                .instruction
                .perf_fee_rate
                .clone()
                .expect("perf_fee_rate is not set"),
            quote_authority: self
                .instruction
                .quote_authority
                .clone()
                .expect("quote_authority is not set"),
            allow_outside_depositors: self
                .instruction
                .allow_outside_depositors
                .clone()
                .expect("allow_outside_depositors is not set"),
            leader_override: self
                .instruction
                .leader_override
                .clone()
                .expect("leader_override is not set"),
        };
        let instruction = CreateVaultCpi {
            __program: self.instruction.__program,

            payer: self.instruction.payer.expect("payer is not set"),

            registry: self.instruction.registry.expect("registry is not set"),

            market: self.instruction.market.expect("market is not set"),

            fee_mint: self.instruction.fee_mint.expect("fee_mint is not set"),

            fee_token_program: self
                .instruction
                .fee_token_program
                .expect("fee_token_program is not set"),

            payer_fee_source: self
                .instruction
                .payer_fee_source
                .expect("payer_fee_source is not set"),

            registry_fee_treasury: self
                .instruction
                .registry_fee_treasury
                .expect("registry_fee_treasury is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            program: self.instruction.program.expect("program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CreateVaultCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    registry: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    fee_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    fee_token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer_fee_source: Option<&'b solana_account_info::AccountInfo<'a>>,
    registry_fee_treasury: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    program: Option<&'b solana_account_info::AccountInfo<'a>>,
    perf_fee_rate: Option<u32>,
    quote_authority: Option<Pubkey>,
    allow_outside_depositors: Option<bool>,
    leader_override: Option<Pubkey>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
