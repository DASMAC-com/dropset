//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::Price;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct VaultDepositorHeader {
    pub discriminator: [u8; 8],
    /// Market PDA this position is on. The vault is identified by
    /// `(market, sector_idx)`; the PDA seeds include both so a sector
    /// reuse across vault lifetimes derives a fresh address.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub market: Pubkey,
    /// Sector index within the market's slab.
    pub sector_idx: u32,
    /// Depositor wallet — bound by the PDA seeds, so the account is
    /// non-transferable (there is no separate authority field to
    /// reassign). See the spec's **Shares**.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub owner: Pubkey,
    /// Pro-rata claim on the vault — the per-depositor term of
    /// invariant I6 (`leader_shares + Σ VaultDepositor.shares ==
    /// total_shares`).
    pub shares: u64,
    /// Quote-denominated principal of the **remaining** position. Spec
    /// formula (`Deposit` top-off): `Σ (quote_in + base_in × ref_now)`.
    /// Reduced on `Withdraw` by the floored share-pro-rata slice.
    pub net_deposits: u64,
    /// Lifetime contributions, monotonic — **never reduced on
    /// withdraw**. Stable denominator for all-time return %.
    pub gross_deposited: u64,
    /// Shares-weighted average reference price across deposits.
    pub entry_ref_price: Price,
    /// Shares-weighted average VPS (`L / total_shares`) across deposits,
    /// Q32.32. Same encoding and practical bound as [`crate::Vault::hwm`].
    pub entry_vps: u64,
    /// Slot of the first deposit. Captured once at PDA init.
    pub opened_at: u64,
    /// Signed quote-denominated PnL crystallized by past withdrawals.
    /// Discarded when the account is closed at zero shares.
    pub realized_pnl: i64,
    /// Signed yield (ex-FX) component of [`Self::realized_pnl`].
    pub realized_yield: i64,
    /// Signed FX component. Invariant:
    /// `realized_yield + realized_fx == realized_pnl`.
    pub realized_fx: i64,
    /// PDA bump.
    pub bump: u8,
    /// Padding so the on-chain size is a multiple of 8 and the
    /// `#[account]` derive's discriminator + body length matches the
    /// const-asserted total below.
    pub reserved: [u8; 7],
}

pub const VAULT_DEPOSITOR_HEADER_DISCRIMINATOR: [u8; 8] = [249, 190, 164, 120, 230, 61, 124, 175];

impl VaultDepositorHeader {
    pub const LEN: usize = 148;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for VaultDepositorHeader {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_vault_depositor_header(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<VaultDepositorHeader>, std::io::Error> {
    let accounts = fetch_all_vault_depositor_header(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_vault_depositor_header(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<VaultDepositorHeader>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<VaultDepositorHeader>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::new(
            std::io::ErrorKind::Other,
            format!("Account not found: {}", address),
        ))?;
        let data = VaultDepositorHeader::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_vault_depositor_header(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<VaultDepositorHeader>, std::io::Error> {
    let accounts = fetch_all_maybe_vault_depositor_header(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_vault_depositor_header(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<VaultDepositorHeader>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<VaultDepositorHeader>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = VaultDepositorHeader::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for VaultDepositorHeader {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for VaultDepositorHeader {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for VaultDepositorHeader {
    fn owner() -> Pubkey {
        crate::DROPSET_ID
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for VaultDepositorHeader {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for VaultDepositorHeader {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
