//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::FeeConfig;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RegistryHeader {
    pub discriminator: [u8; 8],
    /// Default fee for the per-`OpenVault` charge, stamped into
    /// `MarketHeader.fee_config` at market creation.
    pub default_fee_config: FeeConfig,
    /// Number of live markets created against this registry. Incremented
    /// by `create_market`, decremented by `close_market`. `close_registry`
    /// requires this to be zero — the only on-chain witness that no
    /// orphan markets remain, since the program cannot iterate all PDAs
    /// to verify by enumeration. See the architecture spec, **Account
    /// lifecycle and rent reclamation**.
    pub market_count: u32,
    /// Default skin-in-the-game floor (ppm, [`Ppm32`]) stamped into markets.
    pub default_min_leader_share: u32,
    /// Default taker fee (ppm, [`Ppm16`]) stamped into markets.
    pub default_taker_fee: u16,
    /// Default cap on vaults per market.
    pub max_vaults_per_market: u8,
    /// Registry PDA bump.
    pub bump: u8,
}

pub const REGISTRY_HEADER_DISCRIMINATOR: [u8; 8] = [74, 214, 198, 41, 138, 211, 36, 245];

impl RegistryHeader {
    pub const LEN: usize = 92;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for RegistryHeader {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_registry_header(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<RegistryHeader>, std::io::Error> {
    let accounts = fetch_all_registry_header(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_registry_header(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<RegistryHeader>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<RegistryHeader>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::new(
            std::io::ErrorKind::Other,
            format!("Account not found: {}", address),
        ))?;
        let data = RegistryHeader::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_registry_header(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<RegistryHeader>, std::io::Error> {
    let accounts = fetch_all_maybe_registry_header(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_registry_header(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<RegistryHeader>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<RegistryHeader>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = RegistryHeader::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for RegistryHeader {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for RegistryHeader {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for RegistryHeader {
    fn owner() -> Pubkey {
        crate::DROPSET_ID
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for RegistryHeader {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for RegistryHeader {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
